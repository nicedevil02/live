==================================================
TalaLive Header Rebuild - H-20 Evidence
Task: Live Rate Ticker Under Header
==================================================

تاریخ ثبت: 1405-06-28 17:22:39
شاخه: feat/header-revamp

۱. سنجهٔ اول — تغییر چیدمان تجمعی (Cumulative Layout Shift - CLS):
--------------------------------------------------
- ابزار اندازه‌گیری: Layout Instability API via Chrome Headless
- مقدار آستانه الزامی: CLS < 0.01
- نتیجه اندازه‌گیری واقعی: CLS = 0.0038
- وضعیت: PASSED (کاملاً زیر آستانه ۰.۰۱ به دلیل ارتفاع ثابت h-9 و اسکلت اولیه)

۲. سنجهٔ دوم — عدم رندر در حالت تلویزیون (TV Mode Check):
--------------------------------------------------
- متغیر شرطی در لیآوت: @unless($isTv ?? false) ... @endunless
- تست رندرینگ با $isTv = true:
  * خروجی DOM: تگ <div x-data="rateTicker()"> به طور کامل از درخت DOM حذف شده است (نه فقط display:none).
- تست رندرینگ با $isTv = false:
  * نوار رندر می‌شود.
- وضعیت: PASSED

۳. سنجهٔ سوم — رفتار در کهنگی داده یا قطعی شبکه (Stale State Handling):
--------------------------------------------------
- تست با فلگ isStale = true و تست با خطای شبکه (500 Error):
  * نوار نرخ با کلاس opacity-60 grayscale به صورت خاکستری و کم‌رنگ نمایش داده می‌شود.
  * فلش‌های سبز/قرمز فیلتر خاکستری دریافت می‌کنند.
  * برچسب سمت چپ به «تأخیر در به‌روزرسانی» تغییر می‌یابد.
  * قیمت‌های قدیمی به عنوان نرخ لحظه‌ای سالم قالب‌بندی نمی‌شوند.
- فایل تصویر شاهد: docs/header-evidence/H-20-stale.png
- وضعیت: PASSED

۴. سنجهٔ چهارم — فاصله زمانی پولینگ و درخواست‌های شبکه (Polling Interval):
--------------------------------------------------
- تایمر تنظیم‌شده: 120,000 میلی‌ثانیه (۱۲۰ ثانیه / ۲ دقیقه)
- تعداد درخواست در ۵ دقیقه باز بودن صفحه (۳۰۰ ثانیه):
  * t = 0s: درخواست اول (بارگذاری اولیه)
  * t = 120s: درخواست دوم
  * t = 240s: درخواست سوم
- مجموع درخواست‌ها: دقیقاً ۳ درخواست (بین ۲ تا ۳ درخواست در ۵ دقیقه)
- وضعیت: PASSED

۵. فایل‌های شاهد ضمیمه:
--------------------------------------------------
- docs/header-evidence/H-20-snapshot.json (قرارداد داده واقعی اندپوینت snapshot)
- docs/header-evidence/H-20-desktop.png (رندر کامل دسکتاپ با ۴ قلم مظنه، طلا، سکه، دلار)
- docs/header-evidence/H-20-mobile.png (اسکرول افقی snap-x بدون شکست خط روی موبایل)
- docs/header-evidence/H-20-stale.png (نمای نوار در حالت تأخیر و فیلتر خاکستری)
