# طلالایو — پرامپت اجرایی بازسازی هدر سایت (H-01..H-21)

<aside>
⚠️

این سند «نقشهٔ اجرا» است، نه صورت مسئله. ایجنتِ مجری **نباید** نقشهٔ پیاده‌سازی تازه‌ای بنویسد، ترتیب را عوض کند، یا راه‌حل جایگزین اختراع کند. فقط تسک‌ها را به ترتیب شناسه اجرا می‌کند، تیک می‌زند، و در پایان هر فاز توقف می‌کند.

</aside>

## سربرگ اجرا

| قلم | مقدار |
| --- | --- |
| مخزن | `nicedevil02/live` |
| شاخهٔ پایه | `master` |
| شاخهٔ کاری | `feat/header-revamp` (از `master` گرفته شود) |
| فایل اصلی هدف | `resources/views/layouts/public.blade.php` |
| فایل‌های جدید مجاز | `config/navigation.php` · `resources/views/partials/nav-desktop.blade.php` · `resources/views/partials/nav-mobile.blade.php` · `resources/views/partials/rate-ticker.blade.php` · `docs/header-evidence/*` |
| تعداد تسک | ۲۱ (۲۰ اجباری + ۱ اختیاری) |
| فازها | ۴ فاز + دروازهٔ توقف در پایان هر فاز |

## بخش ۰ — قرارداد اجرا (قواعد غیرقابل مذاکره)

1. **نقشهٔ جدید ننویس.** همین سند را مرحله‌به‌مرحله اجرا کن. اگر فکر می‌کنی تسکی اشتباه است، اجرایش نکن و در «دفتر انحرافات» (بخش ۶) ثبت کن و توقف کن.
2. **ترتیب شناسه‌ها را رعایت کن:** `H-01` تا `H-21`. تسک‌ها عمداً طوری چیده شده‌اند که تسک‌های بعدی روی خروجی تسک‌های قبلی بنشینند (مثلاً `H-04` قبل از `H-05` و `H-06` لازم است).
3. **بعد از اتمام هر تسک، چک‌باکس همان تسک را در این سند تیک بزن** و کنارش هش کوتاه کامیت را بنویس. تسک بدون تیک = انجام‌نشده.
4. **روی `master` کامیت نکن.** همه‌چیز روی `feat/header-revamp`. هر فاز = یک کامیت با پیام `fix(header): <فاز> — H-xx..H-yy`.
5. **اگر بلوک «کد فعلی» یک تسک با محتوای واقعی فایل مو‌به‌مو نخواند، توقف کن و گزارش بده.** حدس نزن، کد مشابه را پیدا نکن، و «تقریباً همین است» را نپذیر. یعنی فایل از زمان نوشتن این سند تغییر کرده و باید تصمیم انسانی گرفته شود.
6. **هیچ وابستگی جدیدی اضافه نکن** — نه npm، نه composer، نه CDN، نه فونت یا آیکون‌پک جدید. Alpine و Tailwind موجود کافی‌اند.
7. **هیچ فایلی خارج از فهرست «فایل‌های جدید مجاز» و فایل اصلی هدف را تغییر نده**، مگر تسک صریحاً گفته باشد.
8. **قاعدهٔ شاهد:** هر تسک یک «شاهد» دارد که در ستون شاهدش نوشته شده. شاهد باید به‌صورت فایل در `docs/header-evidence/` کامیت شود (خروجی متنی `grep`، یا تصویر اسکرین‌شات). **تسکی که کد دارد ولی شاهد ندارد، تمام‌نشده حساب می‌شود.** این قاعده از روی تجربهٔ اپ تلویزیون آمده: آن‌جا کد نوشته شد ولی آزمون‌ها اجرا نشدند و سه باگ تا مرحلهٔ ممیزی زنده ماندند.
9. **در پایان هر فاز توقف کن**، گزارش کوتاه بده (تسک‌های تیک‌خورده، شاهدها، انحرافات) و منتظر تأیید بمان. فاز بعدی را خودسرانه شروع نکن.

<aside>
🚫

**سه کاری که اگر بکنی کار را خراب می‌کنی:**

1. بازنویسی کامل هدر از صفر. این سند «جراحی نقطه‌ای» است؛ ساختار فعلی هدر سالم است و باید بماند.
2. تغییر متن‌های فارسی یا آدرس route‌ها به سلیقهٔ خودت. متن‌ها فقط در تسک‌هایی که صریحاً گفته‌اند عوض می‌شوند.
3. اضافه‌کردن کتابخانهٔ کمکی (headlessui، flowbite، آیکون‌پک) برای «تمیزتر شدن». ممنوع.
</aside>

## بخش ۱ — وضعیت فعلی (برای تشخیص هویت فایل)

قبل از شروع، این سه چیز را تأیید کن و نتیجه را در `docs/header-evidence/00-baseline.txt` بنویس:

- [ ]  فایل `resources/views/layouts/public.blade.php` وجود دارد و حجمش حدود ۶۹ کیلوبایت است.
- [ ]  در همان فایل، تگ `body` کلاس `overflow-x-hidden` و `x-data="publicLayoutHandler()"` را دارد.
- [ ]  تابع `publicLayoutHandler` در انتهای فایل، شش کلید بولین دارد: `mobileMenuOpen`, `productsDropdownOpen`, `compareDropdownOpen`, `toolsDropdownOpen`, `guidesDropdownOpen`, `devDropdownOpen`.

اگر هر یک از این سه مورد برقرار نبود → **توقف کامل**، طبق قاعدهٔ ۵.

## بخش ۲ — فاز A: رفع باگ‌های قطعی (H-01 .. H-09)

این فاز هیچ تغییر بصری عمدی ندارد؛ فقط رفتار غلط را درست می‌کند. بعد از این فاز ظاهر هدر باید **عیناً مثل قبل** باشد (به‌جز حذف تپش لوگو در `H-09`).

### H-01 — حذف تناقض `overflow-x` از تگ body

مشکل ریشه‌ای: در بلوک `style` هوشمندانه از `overflow-x: clip` استفاده شده (که نیای عنصر را به scroll-container تبدیل نمی‌کند و `position: sticky` را نمی‌شکند) و برای مرورگرهای قدیمی با `@supports not` به `hidden` عقب‌نشینی می‌کند. اما روی خود تگ `body` کلاس یوتیلیتی `overflow-x-hidden` هم نشسته که همان `clip` را بی‌اثر می‌کند و دقیقاً حالتی را می‌سازد که `@supports` می‌خواست از آن فرار کند. نتیجه: ریسک از کار افتادن چسبندگی هدر در بعضی مرورگرها.

در تگ `body`، این دو کلاس حذف شوند و هیچ کلاس دیگری تغییر نکند:

```
overflow-x-hidden
justify-between
```

(`justify-between` هم حذف می‌شود چون `main` کلاس `flex-grow` دارد و چیدمان را خودش می‌سازد؛ حضور هم‌زمان هر دو باعث رفتار غیرقابل پیش‌بینی در صفحات کوتاه می‌شود.)

**معیار پذیرش رفتاری:** صفحهٔ اصلی را در کروم باز کن، ۲۰۰۰ پیکسل اسکرول کن؛ هدر باید بالای صفحه بچسبد. سپس با DevTools عرض را روی ۳۲۰px بگذار؛ **نوار اسکرول افقی نباید ظاهر شود**.

**شاهد:** `docs/header-evidence/H-01.txt` شامل خروجی جست‌وجوی `overflow-x-hidden` در فایل (باید صفر نتیجه باشد) + فایل `H-01-sticky.png` (اسکرین‌شات هدر چسبیده در وسط اسکرول).

### H-02 — مقاوم‌سازی اسکریپت تم در برابر استوریج مسدود

مشکل ریشه‌ای: اسکریپت تم **اولین اسکریپت `head`** است و `localStorage` را بدون محافظ صدا می‌زند. در وب‌ویو‌هایی که استوریج مسدود است (آیفریم، مرورگر داخلی پیام‌رسان‌ها مثل همان روبیکایی که در فوتر لینک شده) این خط خطا می‌دهد، **کل بلوک اسکریپت می‌میرد**، و در نتیجه `toggleAppTheme` هرگز تعریف نمی‌شود — یعنی دکمهٔ تم در کل سایت خطا می‌دهد.

کل بلوک اسکریپت تم در `head` با این نسخه جایگزین شود:

```html
<script>
    (function () {
        var savedTheme = null;
        try { savedTheme = localStorage.getItem('talalive_theme'); } catch (e) { savedTheme = null; }
        if (savedTheme === 'dark') {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    })();

    function toggleAppTheme() {
        var isDark = document.documentElement.classList.toggle('dark');
        try { localStorage.setItem('talalive_theme', isDark ? 'dark' : 'light'); } catch (e) { /* استوریج مسدود است؛ تم فقط تا پایان همین صفحه می‌ماند */ }
        window.dispatchEvent(new CustomEvent('talalive-theme-changed', { detail: { isDark: isDark } }));
    }
</script>
```

**معیار پذیرش رفتاری:** در کنسول کروم، قبل از لود صفحه، دسترسی به استوریج را با DevTools (تب Application → Storage → مسدودکردن) یا با اجرای `Object.defineProperty(window, 'localStorage', { get() { throw new Error('blocked'); } })` در یک اسکریپت زودتر، مسدود کن. سپس صفحه باید **نرمال لود شود** و دکمهٔ تم باید هنوز تم را عوض کند (فقط ماندگار نباشد).

**شاهد:** `docs/header-evidence/H-02.txt` — کپی خروجی کنسول که نشان دهد هیچ خطای قرمزی نیست و `typeof toggleAppTheme === 'function'` مقدار `true` برمی‌گرداند.

### H-03 — رفع فلش دو آیکون همبرگر

مشکل ریشه‌ای: Alpine با `defer` لود می‌شود، پس تا لحظهٔ init هر دو آیکون (همبرگر و ضربدر) در DOM رندر شده‌اند و روی موبایل یک فلش «دو آیکون روی هم» دیده می‌شود. آیکون اول به‌خاطر منطق `!mobileMenuOpen` باید دیده شود، پس فقط آیکون دوم باید پنهان بماند.

روی تگ `svg` دومِ دکمهٔ همبرگر (آنی که `x-show="mobileMenuOpen"` دارد و مسیرش `M6 18L18 6M6 6l12 12` است)، صفت `x-cloak` اضافه شود.

**معیار پذیرش رفتاری:** در DevTools شبکه را روی `Slow 3G` بگذار و صفحه را روی عرض ۳۷۵px رفرش کن. در تمام مدت لود، **فقط یک آیکون** در گوشهٔ هدر دیده شود.

**شاهد:** `docs/header-evidence/H-03.png` — اسکرین‌شات وسط لود با throttling فعال.

### H-04 — بازنویسی `publicLayoutHandler` (حذف کد مرده + منوی انحصاری + قفل اسکرول)

مشکل ریشه‌ای سه‌تایی: (الف) `darkMode`, `toggleTheme()` و لیسنر `talalive-theme-changed` باقی‌ماندهٔ ریفکتور قدیمی‌اند و **هیچ‌جا مصرف نمی‌شوند** (کنترل آیکون‌ها به CSS منتقل شده و دکمه `onclick="toggleAppTheme()"` صدا می‌زند) — وجودشان باعث می‌شود نفر بعدی دو مسیر موازی تم ببیند و اشتباهی را وصل کند؛ (ب) پنج بولین مستقل اجازه می‌دهند چند دراپ‌داون هم‌زمان باز بمانند؛ (ج) هیچ قفل اسکرولی برای منوی موبایل وجود ندارد.

تابع `publicLayoutHandler` در انتهای فایل **کاملاً** با این نسخه جایگزین شود:

```jsx
function publicLayoutHandler() {
    return {
        mobileMenuOpen: false,
        openDropdown: null,

        isOpen(name) {
            return this.openDropdown === name;
        },
        openMenu(name) {
            this.openDropdown = name;
        },
        closeMenu(name) {
            if (this.openDropdown === name) this.openDropdown = null;
        },
        closeAllMenus() {
            this.openDropdown = null;
        },

        toggleMobileMenu() {
            this.mobileMenuOpen ? this.closeMobileMenu() : this.openMobileMenu();
        },
        openMobileMenu() {
            this.mobileMenuOpen = true;
            document.body.classList.add('menu-open');
        },
        closeMobileMenu() {
            if (!this.mobileMenuOpen) return;
            this.mobileMenuOpen = false;
            document.body.classList.remove('menu-open');
        },
    };
}
```

و این قاعده به بلوک `style` در `head` اضافه شود:

```css
body.menu-open { overflow: hidden; }
```

**معیار پذیرش رفتاری:** در کنسول، `Alpine.$data(document.body).openDropdown` بعد از باز کردن «سامانه‌ها» باید `"products"` باشد و بعد از باز کردن «ابزارها» باید `"tools"` شود (نه هر دو).

**شاهد:** `docs/header-evidence/H-04.txt` — خروجی جست‌وجوی `darkMode` و `toggleTheme` در فایل (باید صفر نتیجه باشد) + کپی خروجی کنسول بالا.

### H-05 — سیم‌کشی پنج دراپ‌داون به منوی انحصاری + بستن با کلیک بیرون و Escape

مشکل ریشه‌ای: دراپ‌داون‌ها فقط با `mouseenter`/`mouseleave` کار می‌کنند. روی دستگاه لمسی با عرض ≥۱۰۲۴px (آیپد افقی، لپ‌تاپ لمسی) رویداد `mouseleave` **هیچ‌وقت رخ نمی‌دهد**؛ کاربر یک منو را باز می‌کند و راهی برای بستنش ندارد، و منوی دوم هم رویش باز می‌شود.

برای **هر پنج** دراپ‌داون، نام کلید قدیمی به نام جدید نگاشت می‌شود:

| کلید قدیمی | نام جدید |
| --- | --- |
| `productsDropdownOpen` | `products` |
| `compareDropdownOpen` | `compare` |
| `toolsDropdownOpen` | `tools` |
| `guidesDropdownOpen` | `guides` |
| `devDropdownOpen` | `dev` |

الگوی تغییر (نمونه برای `products`؛ برای چهار مورد دیگر عیناً با نام خودشان تکرار شود):

در تگ `div` بیرونی، صفات رویداد از این:

```html
<div class="relative" @mouseenter="productsDropdownOpen = true" @mouseleave="productsDropdownOpen = false">
```

به این تغییر کنند:

```html
<div class="relative"
     @mouseenter="openMenu('products')"
     @mouseleave="closeMenu('products')"
     @click.outside="closeMenu('products')"
     @keydown.escape.window="closeMenu('products')">
```

در تگ `button`، صفت کلیک از `@click="productsDropdownOpen = !productsDropdownOpen"` به این تغییر کند:

```html
@click="isOpen('products') ? closeMenu('products') : openMenu('products')"
```

در `svg` فلش، عبارت `:class` از `productsDropdownOpen ? ...` به `isOpen('products') ? ...` تغییر کند (متن کلاس‌ها دست‌نخورده بماند).

در `div` پنل بازشو، `x-show="productsDropdownOpen"` به `x-show="isOpen('products')"` تغییر کند. صفت `x-cloak` و کل بلوک `x-transition` **دست‌نخورده** بماند.

**معیار پذیرش رفتاری:** سه سنجه، همه باید برقرار باشند:

1. با ماوس روی «سامانه‌ها» برو → باز شود؛ ماوس را بیرون ببر → بسته شود (رفتار قبلی حفظ شده).
2. روی «سامانه‌ها» کلیک کن (باز شود)، بعد جایی از بدنهٔ صفحه کلیک کن → بسته شود.
3. روی «سامانه‌ها» کلیک کن، کلید `Escape` را بزن → بسته شود.

و در حالت شبیه‌سازی لمسی (DevTools → Toggle device toolbar با عرض ۱۲۸۰px) روی «سامانه‌ها» بزن و بعد روی «ابزارها» → فقط «ابزارها» باز بماند.

**شاهد:** `docs/header-evidence/H-05.txt` — خروجی جست‌وجوی `DropdownOpen` در فایل (باید صفر نتیجه باشد) + فهرست سه‌گانهٔ بالا با علامت انجام‌شده برای هر پنج منو (۱۵ خط).

### H-06 — اصلاح منوی کشویی موبایل

مشکل ریشه‌ای چهارتایی: بسته‌نشدن با کلیک بیرون، بسته‌نشدن با Escape، اسکرول بدنهٔ صفحه زیر منوی باز (کاربر فکر می‌کند منو را اسکرول می‌کند)، و `max-h-[85vh]` که روی موبایل با نوار آدرس متغیر می‌پرد.

چهار تغییر:

1. در دکمهٔ همبرگر، `@click="mobileMenuOpen = !mobileMenuOpen"` به `@click="toggleMobileMenu()"` تغییر کند.
2. در `div` منوی کشویی، به صفات موجود اضافه شود:

```html
@click.outside="closeMobileMenu()"
@keydown.escape.window="closeMobileMenu()"
```

1. در کلاس همان `div`، `max-h-[85vh]` به این تغییر کند:

```
max-h-[85dvh]
```

1. روی **تگ `nav` والدِ** هر گروه لینک داخل منوی موبایل (پنج گروه: سامانه‌ها، مقایسه‌ها، ابزارها، آموزش، و ردیف درباره/تماس) این صفت اضافه شود تا کلیک روی هر لینک، منو و قفل اسکرول را آزاد کند:

```html
@click="closeMobileMenu()"
```

**معیار پذیرش رفتاری:** روی عرض ۳۷۵px منو را باز کن؛ (الف) تلاش برای اسکرول بدنه در پشت منو نباید صفحه را حرکت دهد، ولی خود منو باید اسکرول شود؛ (ب) `Escape` منو را ببندد و `document.body.classList.contains('menu-open')` مقدار `false` شود؛ (ج) کلیک روی «تعرفه‌ها و اشتراک» هم منو را ببندد هم به صفحهٔ تعرفه برود؛ (د) بعد از هر بستن، اسکرول بدنه دوباره کار کند.

**شاهد:** `docs/header-evidence/H-06.txt` با چهار سنجهٔ بالا و نتیجهٔ هرکدام.

### H-07 — حذف `fetchpriority="high"` از لوگو

مشکل ریشه‌ای: این پرچم به مرورگر می‌گوید تصویر لوگوی ۴۴ پیکسلی را **قبل از** تصویر هیروی صفحه بگیر. لوگو هیچ‌وقت LCP نیست، پس این فقط LCP واقعی را عقب می‌اندازد.

در تگ `img` لوگوی هدر (آنی که کلاس `pulse-logo` دارد)، صفت `fetchpriority="high"` حذف شود. صفت `loading="eager"` **باقی بماند** (لوگو نباید lazy شود چون در viewport اولیه است).

**معیار پذیرش رفتاری:** در Lighthouse روی صفحهٔ اصلی، عدد LCP نباید بدتر از قبل شود و در بخش «Largest Contentful Paint element» عنصر معرفی‌شده نباید لوگو باشد.

**شاهد:** `docs/header-evidence/H-07.txt` — عدد LCP قبل و بعد + نام عنصر LCP.

### H-08 — افزودن `scroll-padding-top`

مشکل ریشه‌ای: `scroll-smooth` روی `html` فعال است ولی هدر `sticky` با ارتفاع ۶۴px (موبایل) و ۸۰px (دسکتاپ) است. هر لینک لنگری داخل صفحات محتوایی، تیتر هدف را **زیر هدر** می‌برد.

به بلوک `style` در `head` اضافه شود:

```css
html { scroll-padding-top: 4.5rem; }
@media (min-width: 640px) {
    html { scroll-padding-top: 5.5rem; }
}
```

**معیار پذیرش رفتاری:** در صفحهٔ «تعرفه‌ها» (یا هر صفحه‌ای که لینک لنگری دارد) روی یک لینک `#` کلیک کن؛ تیتر مقصد باید **کامل زیر هدر و قابل خواندن** باشد، نه پشت آن.

**شاهد:** `docs/header-evidence/H-08.png` — اسکرین‌شات بعد از پرش لنگری.

### H-09 — احترام به `prefers-reduced-motion` و حذف تپش لوگو

مشکل ریشه‌ای: در هدر **همهٔ** صفحات، دو انیمیشن بی‌پایان هم‌زمان اجرا می‌شوند: `pulse-logo` (مقیاس‌دادن لوگو هر ۳ ثانیه) و `animate-ping` (نقطهٔ کهربایی). سه ایراد: حواس‌پرتی از CTA اصلی، مصرف دائمی CPU و باتری روی دستگاه‌های ضعیف (از جمله همان اندروید‌باکس‌هایی که مشتری خودمان‌اند)، و نادیده‌گرفتن تنظیم کاهش حرکت سیستم‌عامل.

دو تغییر:

1. کلاس `pulse-logo` از تگ `img` لوگو حذف شود. خودِ تعریف `@keyframes pulse-glow` و کلاس `.pulse-logo` در بلوک `style` **باقی بماند** (شاید جای دیگری استفاده شود؛ حذفش دامنهٔ این سند نیست). نقطهٔ `animate-ping` **باقی می‌ماند** چون معنای واقعی دارد: «نرخ زنده است».
2. به بلوک `style` اضافه شود:

```css
@media (prefers-reduced-motion: reduce) {
    .pulse-logo,
    .animate-ping,
    .animate-pulse {
        animation: none !important;
    }
    html { scroll-behavior: auto !important; }
}
```

**معیار پذیرش رفتاری:** در DevTools → Rendering → `prefers-reduced-motion: reduce` را فعال کن؛ نقطهٔ کهربایی باید **ثابت** شود (نه محو‌شونده) و پرش لنگری باید فوری باشد نه نرم.

**شاهد:** `docs/header-evidence/H-09.txt` — تأیید دو سنجهٔ بالا + خروجی جست‌وجوی `pulse-logo` که باید فقط **یک** نتیجه (تعریف CSS) داشته باشد، نه دو.

<aside>
🛑

**دروازهٔ توقف فاز A:** کامیت کن با پیام `fix(header): phase A — H-01..H-09`، نه تسک را با شاهدهایشان گزارش بده، و **متوقف شو**. فاز B را شروع نکن تا تأیید بگیری.

</aside>

## بخش ۳ — فاز B: دسترس‌پذیری (H-10 .. H-14)

وضعیت فعلی دسترس‌پذیری هدر تقریباً صفر است. این فاز ارزان‌ترین و بی‌ریسک‌ترین بخش کل سند است: هیچ چیزی از ظاهر عوض نمی‌شود، فقط صفت اضافه می‌شود.

### H-10 — لینک «پرش به محتوا» (skip link)

مشکل ریشه‌ای: هدر ۷ آیتم منو و بیش از ۳۵ لینک دراپ‌داون دارد. کاربر کیبورد یا screen reader باید ده‌ها بار Tab بزند تا به محتوای صفحه برسد.

به‌عنوان **اولین فرزند** تگ `body` (قبل از `header`) این بلوک اضافه شود:

```html
<a href="#main-content"
   class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:right-3 focus:z-[100] focus:px-4 focus:py-2 focus:rounded-xl focus:bg-amber-500 focus:text-slate-950 focus:text-xs focus:font-black focus:shadow-lg">
    پرش به محتوای اصلی
</a>
```

و تگ `main` از `&lt;main class="flex-grow"&gt;` به این تغییر کند:

```html
<main id="main-content" tabindex="-1" class="flex-grow focus:outline-none">
```

**معیار پذیرش رفتاری:** صفحه را لود کن و **یک بار** Tab بزن؛ لینک «پرش به محتوای اصلی» باید دیده شود. Enter بزن؛ فوکوس باید به محتوای اصلی برود و Tab بعدی باید روی اولین لینک محتوا بیفتد، نه روی منو.

**شاهد:** `docs/header-evidence/H-10.png` — اسکرین‌شات لحظهٔ فوکوس روی skip link.

### H-11 — صفات ARIA روی پنج دکمهٔ دراپ‌داون

مشکل ریشه‌ای: screen reader فقط «button» می‌گوید و نمی‌فهمد این دکمه منویی را باز می‌کند یا اینکه منو باز است.

برای هر پنج دکمهٔ دراپ‌داون (با همان نام‌های `products`, `compare`, `tools`, `guides`, `dev` از `H-05`) این سه صفت اضافه شود — نمونه برای `products`:

```html
aria-haspopup="true"
aria-controls="dropdown-products"
:aria-expanded="isOpen('products') ? 'true' : 'false'"
```

و روی `div` پنل بازشوی همان دراپ‌داون، این صفت اضافه شود:

```html
id="dropdown-products"
```

شناسه‌ها به همین الگو: `dropdown-products`, `dropdown-compare`, `dropdown-tools`, `dropdown-guides`, `dropdown-dev`.

**معیار پذیرش رفتاری:** در تب Accessibility در DevTools، روی دکمهٔ «سامانه‌ها» بایست؛ باید `expanded: false` باشد و بعد از کلیک به `expanded: true` تغییر کند. در Lighthouse، بخش Accessibility نباید هیچ خطای `aria-*` روی هدر داشته باشد.

**شاهد:** `docs/header-evidence/H-11.txt` — امتیاز Accessibility در Lighthouse قبل و بعد.

### H-12 — برچسب‌گذاری دکمه‌های بدون متن

مشکل ریشه‌ای: دکمهٔ تم و دکمهٔ همبرگر هیچ متن قابل‌خواندنی ندارند. دکمهٔ تم فقط `title` دارد که برای screen reader قابل اتکا نیست.

سه تغییر:

1. روی دکمهٔ تم (`id="themeToggleBtn"`) اضافه شود: `aria-label="تغییر تم تاریک و روشن"`. صفت `title` موجود باقی بماند (برای tooltip ماوس).
2. روی هر دو تگ `svg` داخل دکمهٔ تم اضافه شود: `aria-hidden="true"`.
3. روی دکمهٔ همبرگر اضافه شود:

```html
aria-label="منوی اصلی"
aria-controls="mobile-menu"
:aria-expanded="mobileMenuOpen ? 'true' : 'false'"
```

و روی `div` منوی کشویی موبایل، صفت `id="mobile-menu"` اضافه شود.

همچنین روی **همهٔ** تگ‌های `svg` تزئینی هدر (فلش‌های دراپ‌داون، آیکون ورود، آیکون‌های همبرگر) صفت `aria-hidden="true"` اضافه شود.

**معیار پذیرش رفتاری:** با screen reader (یا تب Accessibility) روی دکمهٔ همبرگر بایست؛ باید «منوی اصلی، دکمه، collapsed» خوانده شود، نه «button».

**شاهد:** `docs/header-evidence/H-12.txt` — متن accessible name هر سه دکمه از تب Accessibility.

### H-13 — `aria-current` و برچسب ناوبری‌ها

مشکل ریشه‌ای: صفحهٔ فعال فقط با رنگ کهربایی مشخص می‌شود. کاربر screen reader و کاربر کوررنگ هیچ سیگنالی نمی‌گیرند. همچنین دو تگ `nav` در صفحه وجود دارد که از هم قابل تفکیک نیستند.

سه تغییر:

1. روی تگ `nav` دسکتاپ اضافه شود: `aria-label="ناوبری اصلی"`.
2. روی تگ‌های `nav` داخل منوی موبایل اضافه شود: `aria-label="ناوبری موبایل"` روی اولی و برای بقیه همان عنوان گروهشان (مثلاً `aria-label="ماشین‌حساب‌های طلا"`).
3. برای لینک‌هایی که شرط فعال‌بودن بلیدی دارند (صفحه اصلی، تعرفه‌ها، تماس)، داخل همان شرط موجود، علاوه بر کلاس‌های رنگی، صفت `aria-current="page"` هم رندر شود. یعنی شرط فعلی که فقط رشتهٔ کلاس برمی‌گرداند، به شرطی تبدیل شود که هم کلاس و هم این صفت را تولید کند. **منطق شرط را عوض نکن** — فقط خروجی‌اش را گسترش بده.

**معیار پذیرش رفتاری:** در صفحهٔ «تعرفه‌ها»، در DevTools جست‌وجو کن؛ دقیقاً **یک** عنصر با `aria-current="page"` باید وجود داشته باشد و همان لینک تعرفه‌ها باشد.

**شاهد:** `docs/header-evidence/H-13.txt` — خروجی `document.querySelectorAll('[aria-current="page"]').length` در سه صفحهٔ مختلف (باید در هر سه عدد ۱ باشد).

### H-14 — حلقهٔ فوکوس قابل دیدن

مشکل ریشه‌ای: هیچ‌کدام از لینک‌ها و دکمه‌های هدر استایل `focus-visible` ندارند. در بعضی مرورگرها حلقهٔ پیش‌فرض با `rounded-xl` و پس‌زمینهٔ شیشه‌ای تقریباً نامرئی می‌شود.

به بلوک `style` در `head` اضافه شود:

```css
header a:focus-visible,
header button:focus-visible,
footer a:focus-visible {
    outline: 2px solid #f59e0b;
    outline-offset: 2px;
    border-radius: 0.75rem;
}
```

**معیار پذیرش رفتاری:** با کیبورد از ابتدای صفحه Tab بزن و تا انتهای هدر برو؛ در **هر** توقف باید یک حلقهٔ کهربایی واضح دیده شود — هم در تم روشن هم در تم تاریک.

**شاهد:** `docs/header-evidence/H-14.png` — دو اسکرین‌شات (روشن و تاریک) از حلقهٔ فوکوس روی دکمهٔ ثبت‌نام.

<aside>
🛑

**دروازهٔ توقف فاز B:** کامیت با پیام `fix(header): phase B — H-10..H-14`، سپس **توقف و گزارش**. در گزارش، امتیاز Accessibility لایت‌هاوس قبل و بعد را بنویس.

</aside>

## بخش ۴ — فاز C: تجربهٔ کاربری و معماری ناوبری (H-15 .. H-19)

این فاز **ظاهر و ساختار** را عوض می‌کند. اینجا بیشترین ریسک است، پس ترتیب را دقیقاً رعایت کن: اول منبع واحد داده (`H-15`)، بعد چیدمان.

### H-15 — منبع واحد ناوبری (مهم‌ترین تسک این فاز)

مشکل ریشه‌ای: منوی دسکتاپ و منوی موبایل **دو نسخهٔ کاملاً مستقل و دست‌نویس** از یک درخت ناوبری‌اند و متن‌هایشان با هم فرق دارد. نمونه‌های واقعی اختلاف:

| دسکتاپ | موبایل |
| --- | --- |
| تابلو دیجیتال طلا و ارز | تابلو دیجیتال طلا و صرافی |
| مقایسه با تابلوطلا | مقایسه با تابلوطلا دات‌کام |
| دانلود اپلیکیشن | دانلود اپلیکیشن و PWA |
| تبدیل عیارهای طلا (۷۵۰، ۷۰۵...) | تبدیل عیارهای طلا و نقره |

این هم برای سئو بد است (anchor text ناپایدار برای یک URL) هم برای نگهداری (هر تغییر باید دو جا اعمال شود و عملاً نشده).

گام ۱: فایل `config/navigation.php` ساخته شود که یک آرایه برمی‌گرداند. ساختار دقیقاً این باشد:

```php
<?php

return [
    'groups' => [
        [
            'key'   => 'products',
            'label' => 'سامانه‌ها',
            'mobile_heading' => 'سامانه‌ها و تابلوها',
            'width' => 'w-72',
            'items' => [
                [
                    'route' => 'public.smart-gold-board',
                    'icon'  => '💎',
                    'title' => 'تابلوی هوشمند طلافروشی',
                    'desc'  => 'نمایش آنلاین نرخ روی تلویزیون مغازه',
                ],
                // ... بقیهٔ آیتم‌ها با همین سه کلید
            ],
        ],
        // ... چهار گروه دیگر: compare, tools, guides, dev
    ],
];
```

قواعد پرکردن این فایل:

- برای هر لینک، **متن دسکتاپ** به‌عنوان متن رسمی انتخاب شود، **مگر** در چهار ردیف جدول بالا که متن موبایل دقیق‌تر است: در آن چهار مورد متن موبایل انتخاب شود. دلیل: «طلا و صرافی»، «دات‌کام»، «و PWA» و «و نقره» اطلاعات واقعی اضافه می‌کنند.
- هیچ متن جدیدی ننویس. فقط از یکی از دو نسخهٔ موجود انتخاب کن.
- نام route‌ها عیناً از کد فعلی کپی شوند. هیچ route جدیدی نساز.
- ترتیب آیتم‌ها همان ترتیب فعلی دسکتاپ باشد.

گام ۲: دو پارشال `resources/views/partials/nav-desktop.blade.php` و `resources/views/partials/nav-mobile.blade.php` ساخته شوند که روی `config('navigation.groups')` حلقه می‌زنند و **عیناً همان مارک‌آپ و کلاس‌های فعلی** را تولید می‌کنند. هیچ کلاس Tailwind تغییر نکند.

گام ۳: در `public.blade.php`، بلوک‌های دست‌نویس منو با include همان دو پارشال جایگزین شوند.

**معیار پذیرش رفتاری:** قبل از تغییر، HTML رندرشدهٔ صفحهٔ اصلی را ذخیره کن. بعد از تغییر، دوباره ذخیره کن و دیف بگیر. **تنها تفاوت‌های مجاز** همان چهار متنِ جدول بالا هستند (که در دسکتاپ عوض می‌شوند). هر تفاوت دیگری در کلاس‌ها، ترتیب، یا تعداد لینک‌ها = شکست تسک و بازگشت.

**شاهد:** `docs/header-evidence/H-15.diff` — خروجی دیف دو HTML، که باید کوتاه و فقط شامل همان چهار تغییر متنی باشد.

### H-16 — کاهش شلوغی ناوبری دسکتاپ

مشکل ریشه‌ای: `hidden lg:flex` یعنی از ۱۰۲۴px باید همهٔ این‌ها در یک خط جا شوند: لوگو + زیرعنوان + ۷ آیتم منو (۵ تای‌شان با فلش) + دکمهٔ تم + «اتصال تلویزیون مغازه» + «ورود طلافروشان» + «ثبت‌نام گالری». برای همین همه‌چیز به `text-xs` و `px-2.5` و `gap-0.5` فشرده شده — که خودش نشانهٔ سرریز است. روی لپ‌تاپ ۱۳ اینچ (۱۲۸۰px) هم تنگ است.

دو تغییر، هر دو لازم:

1. نقطهٔ شکست ناوبری دسکتاپ از `lg` به `xl` منتقل شود. یعنی در تگ `nav` دسکتاپ، `hidden lg:flex` به `hidden xl:flex`؛ و در دکمهٔ همبرگر و `div` منوی موبایل، `lg:hidden` به `xl:hidden`. **توجه:** این را در هر چهار جایی که `lg:hidden` یا `lg:flex` برای ناوبری استفاده شده اعمال کن و هیچ `lg:` دیگری (مثل `lg:px-8` یا `lg:col-span-1` فوتر) را دست نزن.
2. دو گروه ادغام شوند: گروه `compare` (مقایسه‌ها) به‌عنوان یک بخش دوم **داخل** دراپ‌داون `products` برود، با یک جداکنندهٔ افقی و عنوان کوچک «مقایسه با رقبا». عرض پنل `products` از `w-72` به `w-[32rem]` تغییر کند و محتوایش دو ستونی شود (`grid grid-cols-2 gap-1`): ستون راست تابلوها، ستون چپ مقایسه‌ها. آیتم `dev` (API و ویجت) از ناوبری اصلی حذف شود — این دو لینک همین حالا در فوتر هستند و نقطهٔ ورود اول یک طلافروش نیستند.

نتیجه: ناوبری دسکتاپ از ۷ آیتم به ۵ آیتم می‌رسد: صفحه اصلی · سامانه‌ها (مگامنوی دو ستونی) · ابزارها · آموزش و شهرها · تعرفه‌ها · تماس.

**منوی موبایل دست‌نخورده بماند** — همهٔ گروه‌ها از جمله API و ویجت در آن باقی می‌مانند، چون در منوی کشویی جا هست و هزینه‌ای ندارد.

**معیار پذیرش رفتاری:** در سه عرض ۱۰۲۴px، ۱۲۸۰px و ۱۴۴۰px صفحهٔ اصلی را باز کن:

1. در ۱۰۲۴px باید منوی همبرگری دیده شود (نه ناوبری افقی).
2. در ۱۲۸۰px ناوبری افقی دیده شود و **هیچ آیتمی به خط دوم نرود و هیچ دکمه‌ای بریده نشود**.
3. در ۱۴۴۰px دراپ‌داون «سامانه‌ها» دو ستونی باز شود و **لبهٔ چپش از viewport بیرون نزند**.

**شاهد:** `docs/header-evidence/H-16-1024.png`, `H-16-1280.png`, `H-16-1440.png`.

### H-17 — اندازهٔ ناحیهٔ لمسی

مشکل ریشه‌ای: دکمهٔ تم و همبرگر روی موبایل `w-8 h-8` (۳۲px) هستند و دکمه‌های ورود/ثبت‌نام با `py-1.5` حدود ۲۸px ارتفاع دارند. حداقل توصیه‌شدهٔ ناحیهٔ لمسی ۴۴×۴۴px است. جا هم هست، چون خود هدر ۶۴px ارتفاع دارد.

سه تغییر:

1. در دکمهٔ تم و دکمهٔ همبرگر، `w-8 h-8 sm:w-10 sm:h-10` به `w-11 h-11 sm:w-11 sm:h-11` تغییر کند.
2. در دکمه‌های «ورود» و «ثبت‌نام»، `py-1.5 sm:py-2.5` به `py-2.5` تغییر کند.
3. در دکمهٔ «اتصال تلویزیون»، `py-1.5 sm:py-2.5` به `py-2.5` تغییر کند.

**معیار پذیرش رفتاری:** در DevTools روی عرض ۳۷۵px، ارتفاع محاسبه‌شدهٔ (computed height) هر کدام از دکمهٔ تم، دکمهٔ همبرگر و دکمهٔ ثبت‌نام باید **حداقل ۴۴px** باشد. همچنین هدر نباید بلندتر از ۶۴px شود.

**شاهد:** `docs/header-evidence/H-17.txt` — سه عدد ارتفاع computed + ارتفاع هدر.

### H-18 — کاهش هزینهٔ رندر هدر شیشه‌ای

مشکل ریشه‌ای: `backdrop-blur-xl` (۲۴px) روی نواری که در تمام مدت اسکرول روی محتوا شیشه‌ای می‌ماند، روی اندروید‌های میان‌رده و مهم‌تر روی همان اندروید‌باکس‌هایی که مشتری خودمان هستند، هنگام اسکرول jank می‌سازد.

در تگ `header`، `backdrop-blur-xl` به `backdrop-blur-md` و `bg-white/85 dark:bg-slate-950/85` به `bg-white/95 dark:bg-slate-950/95` تغییر کند. در `div` منوی کشویی موبایل، `backdrop-blur-2xl` به `backdrop-blur-md` تغییر کند.

**معیار پذیرش رفتاری:** در DevTools → Performance، یک ضبط ۵ ثانیه‌ای هنگام اسکرول صفحهٔ اصلی روی پروفایل CPU throttling ۴× بگیر. میانگین FPS باید **بهتر یا مساوی** قبل باشد و تعداد فریم‌های dropped کمتر شود.

**شاهد:** `docs/header-evidence/H-18.txt` — عدد FPS و dropped frames قبل و بعد.

### H-19 — بازآرایی CTA‌های هدر

مشکل ریشه‌ای دوگانه: (الف) دکمهٔ «اتصال تلویزیون مغازه» به `display.tv` می‌رود که صفحهٔ کد جفت‌سازی است — ابزاری برای **مشتری فعلی**، نه بازدیدکنندهٔ جدید. اما جایگاهش در هدر با border دوتایی و ایموجی، پررنگ‌ترین عنصر بعد از ثبت‌نام است و بازدیدکنندهٔ ناشناس رویش می‌زند، صفحهٔ کد می‌بیند و گیج می‌شود. (ب) حالا که اپ اندروید تلویزیون ساخته شده، هدر هیچ لینک برجسته‌ای به آن ندارد و لینک اپ ته دراپ‌داون با فونت ۱۲ قایم شده. (ج) روی موبایل «ورود» با `hidden sm:inline-flex` پنهان است، در حالی که کاربر بازگشتی بیشترین نیاز را به آن دارد.

چهار تغییر:

1. دکمهٔ فعلی «اتصال تلویزیون مغازه» از CTA به لینک ثانویه تنزل کند: کلاس‌های `border-2 border-amber-500/40 bg-amber-500/10` به `border border-slate-300 dark:border-slate-700/80 bg-white/90 dark:bg-slate-900/60` تغییر کند، رنگ متن به `text-slate-700 dark:text-slate-200`، و متنش از «اتصال تلویزیون مغازه» به **«اتصال دستگاه»** تغییر کند. ایموجی 📺 باقی بماند.
2. یک CTA جدید **قبل از** دکمهٔ ثبت‌نام اضافه شود که به route اپ می‌رود (همان route‌ای که در دراپ‌داون با عنوان «دانلود اپلیکیشن» استفاده شده) با متن «دانلود اپ تلویزیون» و استایل کهربایی‌ای که از دکمهٔ «اتصال تلویزیون» آزاد شد (`border-2 border-amber-500/40 bg-amber-500/10 text-amber-700 dark:text-amber-300`). روی موبایل با `hidden md:inline-flex` پنهان شود تا جا تنگ نشود.
3. دکمهٔ «ورود» روی موبایل **به‌صورت آیکونی** دیده شود: `hidden sm:inline-flex` به `inline-flex` تغییر کند و متن داخلش داخل یک `span` با کلاس `hidden sm:inline` پیچیده شود، طوری که زیر ۶۴۰px فقط آیکون ورود دیده شود. برای screen reader روی همان لینک `aria-label="ورود طلافروشان"` اضافه شود.
4. یک لینک تلفن اضافه شود، بین دکمهٔ تم و «اتصال دستگاه»: آیکون تلفن + شماره، که روی موبایل فقط آیکون است (`href="tel:09187009064"`، همان شمارهٔ فوتر). برای یک کسب‌وکار B2B محلی که مخاطبش طلافروش سنتی است، تماس تلفنی کانال تبدیل شمارهٔ یک است و الان فقط در فوتر وجود دارد. با `aria-label="تماس تلفنی با پشتیبانی"`.

**معیار پذیرش رفتاری:** در سه عرض:

1. ۳۷۵px: باید این‌ها دیده شوند — لوگو، تم، تلفن (آیکونی)، ورود (آیکونی)، ثبت‌نام، همبرگر. **هیچ عنصری نباید بریده یا در خط دوم باشد.**
2. ۷۶۸px: «دانلود اپ تلویزیون» ظاهر شود.
3. ۱۲۸۰px: همه‌چیز در یک خط با فاصلهٔ راحت.

**شاهد:** `docs/header-evidence/H-19-375.png`, `H-19-768.png`, `H-19-1280.png`.

<aside>
🛑

**دروازهٔ توقف فاز C:** کامیت با پیام `fix(header): phase C — H-15..H-19`، سپس **توقف و گزارش**. در گزارش، هر ۹ اسکرین‌شات این فاز را فهرست کن. فاز D را شروع نکن تا تأیید بگیری.

</aside>

## بخش ۵ — فاز D: نوار نرخ زنده (H-20)

این تنها تسک این سند است که چیز جدیدی می‌سازد، نه چیز موجودی را درست کند. و مهم‌ترین فرصت هدر هم همین است.

<aside>
💡

**چرا این مهم است:** محصول شما «نمایش نرخ لحظه‌ای طلا» است، ولی صفحهٔ اول سایت **دربارهٔ** آن حرف می‌زند به‌جای اینکه آن را **نشان بدهد**. نوار نرخ زیر هدر چهار کار هم‌زمان می‌کند: اثبات محصول در ثانیهٔ اول؛ دلیلی برای بازگشت روزانه (همان مدل ترافیکی که tgju ازش زندگی می‌کند)؛ محتوای تازه برای سئو؛ و هزینهٔ پیاده‌سازی نزدیک صفر، چون اندپوینت `snapshot` کش‌خوان محض از قبل ساخته شده و آماده است.

</aside>

### H-20 — نوار نرخ زنده زیر هدر

فایل جدید `resources/views/partials/rate-ticker.blade.php` ساخته شود و در `public.blade.php` دقیقاً **بلافاصله بعد از تگ بستهٔ `header` و قبل از `main`** include شود.

**چهار قید اجباری که حذف هیچ‌کدامشان مجاز نیست:**

1. **ارتفاع ثابت و اسکلت اولیه.** نوار از اولین فریم با ارتفاع ثابت `h-9` رندر شود و تا رسیدن داده، محتوای اسکلتی نشان دهد. **هیچ CLS مجاز نیست.** هدر sticky است؛ اگر نوار بعد از لود ارتفاع بگیرد، کل صفحه می‌پرد.
2. **در حالت تلویزیون رندر نشود.** کل بلوک داخل شرط معکوس همان متغیر `isTv` قرار بگیرد که در فاز ۲ اپ تلویزیون به ویو پاس داده می‌شود. تابلوی مغازه نباید دو نرخ‌نما داشته باشد.
3. **حالت کهنگی داده باید دیده شود.** اندپوینت `snapshot` سه کلید `dataAgeSeconds`، `isStale` و `serverTime` را برمی‌گرداند. اگر `isStale` بود، نوار باید **خاکستری** شود و برچسب «تأخیر در به‌روزرسانی» نشان دهد. **درسی که قبلاً گرفتیم:** دادهٔ خراب را هرگز مثل دادهٔ سالم نشان نده — طلافروشی که روی نرخ ۲۰ دقیقه پیش معامله کند، دیگر مشتری شما نیست.
4. **صفر وابستگی جدید و صفر تغییر بک‌اند.** فقط از همان اندپوینت `snapshot` موجود مصرف کن. **هیچ کوئری دیتابیسی در لیآوت نزن** — لیآوت روی تک‌تک صفحات سایت اجرا می‌شود.

محتوای نوار: چهار قلم ثابت به ترتیب — مظنه مثقال · گرم طلای ۱۸ عیار · سکه امامی · دلار. هر قلم شامل عنوان کوتاه، قیمت، و فلش صعود/نزول با رنگ سبز/قرمز. در انتهای نوار (سمت چپ) زمان آخرین به‌روزرسانی. روی موبایل نوار با `overflow-x-auto` و `snap-x` افقی اسکرول شود (نه wrap و نه فشرده).

الگوی اسکلت و کلاینت (اسکلت Alpine مستقل، داخل `publicLayoutHandler` نرود):

```html
<div x-data="rateTicker()" x-init="load()"
     class="h-9 border-b border-slate-200/70 dark:border-slate-800/70 bg-white/90 dark:bg-slate-950/90 overflow-hidden">
    <div class="max-w-7xl mx-auto px-2.5 sm:px-6 lg:px-8 h-9 flex items-center gap-4 overflow-x-auto snap-x scrollbar-none"
         :class="stale ? 'opacity-60 grayscale' : ''">
        <template x-if="!loaded">
            <div class="flex items-center gap-4" aria-hidden="true">
                <div class="h-3 w-24 rounded bg-slate-200 dark:bg-slate-800"></div>
                <div class="h-3 w-24 rounded bg-slate-200 dark:bg-slate-800"></div>
                <div class="h-3 w-24 rounded bg-slate-200 dark:bg-slate-800"></div>
                <div class="h-3 w-24 rounded bg-slate-200 dark:bg-slate-800"></div>
            </div>
        </template>
        <template x-if="loaded">
            <div class="flex items-center gap-4 text-[11px] sm:text-xs whitespace-nowrap">
                <template x-for="row in items" :key="row.key">
                    <span class="flex items-center gap-1.5 snap-start">
                        <span class="text-slate-500 dark:text-slate-400" x-text="row.label"></span>
                        <span class="font-bold text-slate-800 dark:text-slate-100" x-text="row.price"></span>
                        <span :class="row.dir === 'up' ? 'text-emerald-500' : (row.dir === 'down' ? 'text-red-500' : 'text-slate-400')"
                              x-text="row.dir === 'up' ? '▲' : (row.dir === 'down' ? '▼' : '–')"></span>
                    </span>
                </template>
                <span class="text-[10px] text-slate-400 mr-auto" x-text="stale ? 'تأخیر در به‌روزرسانی' : updatedLabel"></span>
            </div>
        </template>
    </div>
</div>
```

و اسکریپت همین پارشال (در `@push('scripts')` همین فایل):

```jsx
function rateTicker() {
    return {
        loaded: false,
        stale: false,
        items: [],
        updatedLabel: '',
        timer: null,

        async load() {
            try {
                const res = await fetch(this.endpoint, { headers: { 'Accept': 'application/json' } });
                if (!res.ok) throw new Error('bad status');
                const data = await res.json();
                this.items = this.mapRows(data);
                this.stale = data.isStale === true;
                this.updatedLabel = this.formatAge(data.dataAgeSeconds);
                this.loaded = true;
            } catch (e) {
                // خطا را بلع نکن: نوار در حالت کهنه باقی می‌ماند، نه در حالت سالم
                this.stale = true;
                this.loaded = this.items.length > 0;
            }
            clearTimeout(this.timer);
            this.timer = setTimeout(() => this.load(), 120000);
        },
    };
}
```

دو نکتهٔ پیاده‌سازی:

- مقدار `endpoint` را از همان route موجود `snapshot` با سینتکس echo بلید در یک صفت `data-endpoint` روی عنصر بیرونی بگذار و در `x-init` بخوان. **آدرس را داخل جاوااسکریپت هاردکد نکن.**
- توابع `mapRows` و `formatAge` را خودت طبق ساختار واقعی خروجی `snapshot` بنویس. **قبل از نوشتن، خروجی واقعی اندپوینت را یک بار بگیر و عیناً در `docs/header-evidence/H-20-snapshot.json` ذخیره کن.** این دقیقاً همان جایی است که در اپ تلویزیون اشتباه شد: UI سیم‌کشی شد ولی قرارداد داده حدس زده شد و درخواست همیشه ۴۲۲ می‌داد.

**معیار پذیرش رفتاری — چهار سنجه:**

1. در Lighthouse صفحهٔ اصلی، **CLS باید زیر ۰٫۰۱ باشد** و نسبت به قبل از این تسک بدتر نشده باشد.
2. باز کردن تابلوی یک کاربر با پارامتر تلویزیونی: نوار **اصلاً در DOM نباشد** (نه فقط مخفی).
3. سرویس نرخ را موقتاً قطع کن (یا در DevTools درخواست را بلاک کن): نوار باید **خاکستری با متن تأخیر** شود، نه خالی و نه قیمت قدیمی با ظاهر سالم.
4. در تب Network، در ۵ دقیقه باز ماندن صفحه، دقیقاً **دو تا سه درخواست** به `snapshot` دیده شود (فاصلهٔ ۱۲۰ ثانیه)، نه بیشتر.

**شاهد:** `docs/header-evidence/H-20.txt` (چهار سنجه) + `H-20-snapshot.json` + `H-20-desktop.png` + `H-20-mobile.png` + `H-20-stale.png`.

### H-21 — (اختیاری) سال پویای کپی‌رایت فوتر

در فوتر، سال **۱۴۰۵ هاردکد شده**. در فروردین بعد، کل سایت به نظر رهاشده می‌رسد — برای سایتی که محصولش «نرخ لحظه‌ای» است، این سیگنال بدی است. با همان تابع تبدیل تاریخ جلالی که در مدل کاربر وجود دارد، سال پویا شود.

**معیار پذیرش:** تاریخ سیستم را موقتاً جلو ببر؛ سال فوتر باید خودبه‌خود عوض شود.

**شاهد:** `docs/header-evidence/H-21.txt`.

<aside>
🛑

**دروازهٔ توقف فاز D:** کامیت با پیام `feat(header): phase D — H-20..H-21`، سپس **توقف** و درخواست بازبینی نهایی. پول‌ریکوئست را خودت مرج نکن.

</aside>

## بخش ۶ — دفتر انحرافات

هر جا مجبور شدی از متن این سند فاصله بگیری، **قبل از ادامه** یک ردیف به این جدول اضافه کن. جدول خالی در پایان کار یعنی یا هیچ انحرافی نبوده، یا ثبت نکرده‌ای — و دفعهٔ قبل دومی بود.

| شناسه | چه کاری خواسته شده بود | چه کاری کردم | چرا |
| --- | --- | --- | --- |
|  |  |  |  |

## بخش ۷ — خارج از دامنه (دست نزن)

این موارد مشکلات واقعی اند، ولی **در این دور انجام نمی‌شوند**. اگر حین کار به‌شان برخوردی، فقط در گزارش اشاره کن و رد شو:

- **جست‌وجو در هدر.** با حدود ۵۰ صفحهٔ محتوایی واقعاً لازم است، ولی پروژهٔ جداگانه‌ای است (ایندکس، صفحهٔ نتایج، رتبه‌بندی) و جایش در این سند نیست.
- **هدر پنل ادمین** (`resources/views/admin/`) و **هدر صفحهٔ تابلو** (`resources/views/display/`). لیآوت‌های جداگانه‌ای دارند.
- **تبدیل لوگو به SVG درون‌خطی.** بهتر از `H-07` است ولی فایل طراحی لازم دارد.
- **بازنگری کلی فوتر.** فقط `H-21` اختیاری فوتر را لمس می‌کند.
- **تغییر Schema.org.** سالم است؛ دست نزن.

## بخش ۸ — چک‌لیست پایانی (تیک بزن)

**فاز A — رفع باگ**

- [x]  `H-01` حذف `overflow-x-hidden` و `justify-between` از body (`bd9a5db`)
- [x]  `H-02` `try/catch` دور `localStorage` در اسکریپت تم (`bd9a5db`)
- [x]  `H-03` `x-cloak` روی آیکون ضربدر (`bd9a5db`)
- [x]  `H-04` بازنویسی `publicLayoutHandler` (`bd9a5db`)
- [x]  `H-05` پنج دراپ‌داون به منوی انحصاری + کلیک بیرون + Escape (`bd9a5db`)
- [x]  `H-06` اصلاح منوی موبایل (قفل اسکرول، `dvh`، Escape، بستن با کلیک) (`bd9a5db`)
- [x]  `H-07` حذف `fetchpriority` از لوگو (`bd9a5db`)
- [x]  `H-08` `scroll-padding-top` (`bd9a5db`)
- [x]  `H-09` `prefers-reduced-motion` + حذف تپش لوگو (`bd9a5db`)
- [x]  کامیت فاز A + گزارش + توقف (`bd9a5db`)

**فاز B — دسترس‌پذیری**

- [x]  `H-10` skip link + `main` با شناسه
- [x]  `H-11` `aria-expanded`/`aria-haspopup`/`aria-controls` روی پنج دراپ‌داون
- [x]  `H-12` `aria-label` دکمهٔ تم و همبرگر + `aria-hidden` آیکون‌ها
- [x]  `H-13` `aria-current="page"` + برچسب ناوبری‌ها
- [x]  `H-14` حلقهٔ فوکوس قابل دیدن
- [x]  کامیت فاز B + گزارش + توقف

**فاز C — تجربهٔ کاربری**

- [ ]  `H-15` منبع واحد ناوبری در `config/navigation.php` + دو پارشال
- [ ]  `H-16` انتقال نقطهٔ شکست به `xl` + ادغام مقایسه‌ها در مگامنو
- [ ]  `H-17` ناحیهٔ لمسی ۴۴px
- [ ]  `H-18` کاهش بلور هدر
- [ ]  `H-19` بازآرایی CTA‌ها + تلفن + ورود روی موبایل
- [ ]  کامیت فاز C + گزارش + توقف

**فاز D — نوار نرخ**

- [ ]  `H-20` نوار نرخ زنده زیر هدر
- [ ]  `H-21` (اختیاری) سال پویای فوتر
- [ ]  کامیت فاز D + گزارش نهایی + باز کردن پول‌ریکوئست (بدون مرج)