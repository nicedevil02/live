# پرامپت اجرایی ایجنت — پیاده‌سازی سئو طلالایو 
# شروع پرامپت

## ۰. قواعد اجرا — این بخش بر همه چیز اولویت دارد

تو یک ایجنت مجری هستی، نه مشاور و نه برنامه‌ریز. این سند **خودِ نقشه اجرا** است و قبلاً تأیید شده.

<aside>
⛔

**ممنوعیت‌های مطلق:**
۰.۱ — برای خودت نقشه راه، پلن، رودمپ، استراتژی یا فایل `PLAN.md` / `ROADMAP.md` / `STRATEGY.md` ننویس. نقشه همین سند است.
۰.۲ — ترتیب گام‌ها را عوض نکن، گام‌ها را ادغام نکن، و چند گام را هم‌زمان انجام نده. دقیقاً یکی‌یکی و به ترتیب شماره.
۰.۳ — گام را حذف یا «غیرضروری» اعلام نکن. اگر گامی واقعاً غیرقابل‌اجراست، طبق قاعده ۰.۷ توقف کن و بپرس.
۰.۴ — جایگزین پیشنهاد نده و معماری بهتر پیشنهاد نده. اگر ایده‌ای داری، در گزارش پایانی همان گام در یک خط بنویس و بدون اجرا رد شو.
۰.۵ — هیچ عدد، آمار، درصد، امتیاز یا نظر مشتری از خودت نساز. جاهای خالی داده واقعی را با `TODO(data): توضیح` علامت بزن و رد شو.

</aside>

**۰.۶ — سازوکار تیک زدن (اجباری):** هر گام یک چک‌باکس دارد. چرخه کار برای هر گام دقیقاً این است:

1. گام را بخوان و **فقط همان گام** را اجرا کن.
2. «معیار پذیرش» همان گام را تست کن.
3. چک‌باکس آن گام را در همین صفحه از `- [ ]` به `- [x]` تغییر بده. اگر به نوشن دسترسی نداری، فایل `SEO_PROGRESS.md` را در ریشه ریپو بساز، همین فهرست گام‌ها را در آن کپی کن و تیک را آنجا بزن.
4. گزارش کوتاه همان گام را طبق قالب بخش ۵ بنویس.
5. **بدون اجازه گرفتن** برو سراغ گام بعدی.

**۰.۷ — شرایط توقف (فقط این سه مورد):** اگر یکی از این‌ها پیش آمد، کار را متوقف کن، دقیقاً بگو کجا ایستادی و سؤالت چیست:

- فایل یا روتی که گام به آن اشاره دارد در ریپو وجود ندارد و معادل روشنی هم ندارد.
- اجرای گام نیاز به داده‌ای دارد که فقط صاحب کسب‌وکار دارد (عکس واقعی، قیمت واقعی، نظر مشتری، دسترسی سرور).
- گام با یک رفتار موجود سایت تعارض جدی دارد و اجرایش چیزی را می‌شکند.

در غیر این سه حالت، توقف نکن و سؤال نپرس.

**۰.۸ — قواعد کد:** حداقل دیف. هیچ تغییر غیرمرتبطی نده. هیچ متن فارسی را ترجمه نکن. نام روت‌ها و ساختار `layouts/public` را نشکن. فایل اضافه نساز مگر گام صریحاً خواسته باشد.

---

## ۱. زمینه پروژه (همه چیزی که لازم داری)

| مورد | مقدار |
| --- | --- |
| ریپو | `nicedevil02/live` — برنچ `main` |
| استک | Laravel + Blade + Tailwind + Alpine (لوکال)، فونت وزیرمتن، PHP 8.4 روی cPanel |
| دامنه | `https://talalive.ir` |
| برند | طلالایو / TalaLive |
| محصول | سامانه ابری نمایش نرخ لحظه‌ای طلا، سکه و ارز روی تلویزیون طلافروشی — بدون سخت‌افزار اختصاصی، اشتراکی (B2B SaaS) |
| زبان | فارسی، RTL |
| دیپلوی | `.cpanel.yml` |

**روت‌های عمومی فعلی:**

```
/  /tv  /p/{code}  /smart-gold-board  /led-vs-smart-board  /pricing
/tv-setup-guide  /about  /contact  /terms  /privacy  /gold-calculator
/tools/gold-price-calculator  /tools/coin-bubble  /tools/mesghal
/tools/melted-gold  /tools/karat-converter
/guides  /guides/{slug}  /cities/{city}
/{username}   ← روت catch-all، همیشه باید آخرین روت باشد
/sitemap.xml  + sitemap-pages|tools|guides|cities|shops.xml
/admin/*  (دارای میدل‌ور noindex)
```

**مقاله‌های موجود:** `gold-price-formula-18k`، `gold-tax-regulations`، `best-tv-for-jewelry-shop`، `how-to-calculate-coin-bubble`

**شهرهای موجود:** tehran, isfahan, mashhad, tabriz, shiraz, yazd, hamedan, qom, ahvaz, rasht

<aside>
🚨

**تله‌ای که می‌تواند کل کار را بی‌سروصدا از بین ببرد:** روت `/{username}` یک catch-all است. هر روت جدیدی که **زیر** آن ثبت شود هرگز اجرا نمی‌شود و سایت به‌جای صفحه، پروفایل گالری یا ۴۰۴ برمی‌گرداند. همه روت‌های جدید باید **بالای** آن تعریف شوند. بعد از هر گامی که روت اضافه می‌کند، این را با `php artisan route:list` تأیید کن.

</aside>

---

## ۲. داده ثابت — این‌ها را عیناً استفاده کن و از خودت نساز

### ۲.۱ واژگان تخصصی صنف که باید در متن صفحات بیاید

این اصطلاحات از متن واقعی رقبا استخراج شده‌اند و سیگنال تخصص موضوعی می‌دهند. در هر صفحه‌ای که موضوعاً مرتبط است، این‌ها را **به‌صورت طبیعی در جمله** به کار ببر — نه به شکل فهرست کلمات و نه با تکرار مصنوعی:

- **محصول:** دستگاه اعلام نرخ • تابلو آنلاین • نرخ زن • نرخ نامه دیجیتالی • مینی اسمارت • فول کالر • سون سگمنت • ۷ رقمه • روز دید / سایه دید • دانگل • اتصال HDMI • مینی‌کامپیوتر تک‌منظوره
- **نرخ:** مظنه تهران • مظنه بازار • مظنه مثقال • مظنه فردایی • مظنه جهانی • تعویض متفرقه ۱۸ • خرید متفرقه ۱۸ • آبشده نقدی • نرخ مرجع • نرخ اتحادیه • طلای دست دوم • عیار ۷۰۵ / ۷۴۰ / ۷۵۰ / ۹۹۹
- **مسکوکات:** سکه امامی • بهار آزادی • طرح قدیم / جدید • نیم سکه • ربع سکه • سکه گرمی • سکه پارسیان • شمش طلا • شمش نقره • گرم نقره ۹۹۹ / ۹۹۵ / ۹۲۵

### ۲.۲ جدول تخصیص صفحه ← کلمه ← تایتل (منبع حقیقت)

هر صفحه **فقط یک** کلمه هدف اصلی دارد. تایتل و دیسکریپشن را عیناً از این جدول بردار.

| URL | کلمه هدف اصلی | کلمات پشتیبان | Title |
| --- | --- | --- | --- |
| `/digital-rate-board` | نرخ نامه دیجیتال طلافروشی | تابلو نرخ نامه • نرخ نامه دیجیتالی • تابلو ۷ رقمه طلافروشی | نرخ نامه دیجیتال طلافروشی — بدون تابلوی ۷ رقمه | طلالایو |
| `/gold-board-without-device` | تابلو طلا بدون دستگاه | تابلو طلا بدون نصب • بدون کیس • بدون سخت‌افزار | تابلو طلا بدون دستگاه — فقط تلویزیون و اینترنت | طلالایو |
| `/guides/mazaneh-fardaei` | مظنه فردایی چیست | مظنه نقدی • مظنه جهانی • مظنه مثقال | مظنه فردایی چیست و چه فرقی با مظنه نقدی دارد؟ |
| `/guides/motefareghe-18` | تعویض متفرقه ۱۸ | خرید متفرقه ۱۸ • متفرقه چیست • نرخ خرید طلای متفرقه | تعویض و خرید متفرقه ۱۸ چیست؟ راهنمای طلافروش |
| `/guides/goldsmith-legal-profit` | سود قانونی طلافروشی | درصد سود طلافروش • مصوبه اتحادیه • اجرت و سود | سود قانونی طلافروشی چند درصد است؟ (۱۴۰۵) |
| `/online-gold-price-board` | تابلو آنلاین قیمت طلا | تابلو آنلاین طلا و مسکوکات • تابلو قیمت طلا • تابلو لحظه‌ای | تابلو آنلاین قیمت طلا و مسکوکات — نرخ لحظه‌ای | طلالایو |
| `/guides/led-board-price-1405` | قیمت تابلو ال ای دی طلافروشی | لیست قیمت تابلو روان • ماژول P10 • قیمت متری تابلو | قیمت تابلو ال ای دی طلافروشی در ۱۴۰۵ — راهنمای کامل |
| `/compare/tabangohar` | تابان گوهر یا طلالایو | دستگاه اسمارت • مینی اسمارت • شارژ سالیانه | تابان گوهر یا طلالایو؟ مقایسه هزینه و امکانات |
| `/compare/tgju-tv` | تابلو هوشمند TGJU یا طلالایو | تابلو قیمت رایگان • تابلو بدون نصب | تابلو هوشمند TGJU یا طلالایو؟ تفاوت‌های واقعی |
| `/compare/tablotala` | اپ تابلو طلا یا طلالایو | تابلو طلا اندروید • دانلود تابلو طلا | اپ تابلو طلا یا طلالایو؟ مقایسه برای طلافروش |
| `/tools/wage-calculator` | محاسبه اجرت طلا | درصد اجرت • جدول اجرت • اجرت ساخت | محاسبه اجرت طلا — جدول درصد اجرت ۱۴۰۵ |
| `/tools/second-hand-gold` | قیمت‌گذاری طلای دست دوم | طلای مستعمل • کسر افت • طلای کارکرده | قیمت‌گذاری طلای دست دوم — محاسبه‌گر آنلاین |
| `/guides/gold-hallmark-inquiry` | استعلام انگ طلا | ری گیری طلا • شماره پاکت • جواب انگ | استعلام انگ طلا و ری‌گیری — راهنمای کامل |
| `/currency-exchange-board` | تابلو صرافی و نرخ ارز | تابلو نرخ ارز • تابلو LED صرافی • تابلو منو نرخ | تابلو صرافی و نرخ ارز روی تلویزیون — بدون LED |
| `/silver-bullion-board` | تابلو نرخ نقره و شمش | قیمت گرم نقره ۹۹۹ • تابلو شمش | تابلو نرخ نقره و شمش — گرم ۹۹۹، ۹۹۵ و ۹۲۵ |
| `/android-tv-gold-board` | تابلو طلا روی اندروید تی‌وی | تابلو طلا اندروید • باکس اندروید • دانگل | تابلو طلا روی اندروید تی‌وی — راه‌اندازی ۳ دقیقه‌ای |
| `/guides/samsung-tizen-gold-board` | تابلو قیمت طلا سامسونگ | تایزن • مرورگر تلویزیون سامسونگ | تابلو قیمت طلا روی تلویزیون سامسونگ (Tizen) |
| `/guides/lg-webos-gold-board` | تابلو قیمت طلا ال‌جی | webOS • مرورگر تلویزیون ال‌جی | تابلو قیمت طلا روی تلویزیون ال‌جی (webOS) |
| `/demo` | نمونه تابلو طلا فروشی | دمو تابلو طلا • پیش‌نمایش تابلو | نمونه زنده تابلو طلا فروشی — بدون ثبت‌نام |
| `/widget` | ویجت قیمت طلا برای سایت | امبد نرخ طلا • iframe قیمت طلا | ویجت رایگان قیمت طلا و سکه برای سایت شما |
| `/api-docs` | وب سرویس قیمت طلا | API قیمت طلا و ارز • REST نرخ طلا | وب سرویس و API قیمت طلا، سکه و ارز | طلالایو |
| `/app` | اپلیکیشن تابلو طلا | دانلود تابلو طلا • اپ نرخ طلا | اپلیکیشن تابلو طلا — اندروید و اندروید تی‌وی |

### ۲.۳ تخصیص کلمه به صفحات موجود (برای گام‌های بهینه‌سازی)

| URL | کلمه هدف اصلی |
| --- | --- |
| `/` | طلالایو (برند) |
| `/smart-gold-board` | تابلوی هوشمند طلافروشی |
| `/led-vs-smart-board` | تابلو ال ای دی طلا فروشی |
| `/pricing` | قیمت تابلو طلا فروشی |
| `/tv-setup-guide` | تابلو قیمت طلا برای تلویزیون |
| `/gold-calculator` | ماشین حساب طلا |
| `/tools/gold-price-calculator` | محاسبه قیمت طلا با اجرت و مالیات |
| `/tools/coin-bubble` | حباب سنج سکه |
| `/tools/mesghal` | تبدیل مظنه به گرم |
| `/tools/melted-gold` | محاسبه طلای آبشده |
| `/tools/karat-converter` | تبدیل عیار طلا |
| `/cities/{city}` | تابلو طلا فروشی + نام شهر |
| `/{username}` | قیمت لحظه‌ای طلا + نام گالری + شهر |

### ۲.۴ خطوط قرمز محتوایی

- **هیچ** `aggregateRating` یا امتیاز و تعداد رأی در اسکیما ننویس.
- **هیچ** `meta keywords` در هیچ صفحه‌ای نگذار.
- عدد «صرفه‌جویی ۴۰ میلیون تومان» و بازه «۲۵ تا ۵۵ میلیون تومان» برای تابلو LED را در هیچ صفحه‌ای تکرار نکن؛ داده بازار این را تأیید نمی‌کند. به‌جایش از بازه مستند «حدود ۸ تا ۳۰ میلیون تومان در هر متر مربع، بسته به نوع ماژول» با ذکر «قیمت‌ها تقریبی و متغیر است» استفاده کن.
- ادعای «نخستین»، «پیشرفته‌ترین»، «۹۸٪ گالری‌ها»، «آپ‌تایم ۹۹.۹٪» و «CPU زیر ۳ درصد» را در متن جدید تکرار نکن.
- درباره رقبا فقط چیزی بنویس که از صفحه عمومی خودشان قابل اثبات است. صفت منفی ارزشی («بی‌کیفیت»، «کلاهبرداری») ممنوع.
- متن تکراری بین صفحات ممنوع؛ هر صفحه باید حداقل یک جدول، ابزار یا داده مخصوص خودش داشته باشد.

---

## ۳. تعریف «انجام‌شده» برای هر صفحه

هر صفحه‌ای که در گام‌ها می‌سازی یا بازنویسی می‌کنی، قبل از تیک خوردن باید هر ۱۲ مورد را داشته باشد:

1. کلمه هدف اصلی از جدول ۲.۲ یا ۲.۳ در `config/seo.php` ثبت شده
2. تایتل عیناً از جدول، زیر ۶۰ کاراکتر مفید، برند در انتها
3. دیسکریپشن ۱۴۰ تا ۱۶۰ کاراکتر با یک عدد و یک فراخوان
4. دقیقاً یک `H1` که کلمه هدف را دارد و کپی تایتل نیست
5. تعریف صریح موضوع در ۴۰ کلمه اول متن
6. حداقل دو `H2` با ساختار منطقی
7. حداقل سه کلمه پشتیبان به‌صورت طبیعی در متن
8. حداقل یک عنصر یونیک: جدول داده، ماشین‌حساب، یا اسکرین‌شات
9. حداقل سه لینک داخلی خروجی و دو لینک داخلی ورودی از صفحات دیگر
10. اسکیمای درست: `Article` برای مقاله، `Service` برای صفحه فروش — بدون rating
11. `canonical` مطلق روی `https` و حضور در نقشه سایت
12. بلوک «مطالب مرتبط» با سه لینک و یک CTA میانی

---

## ۴. گام‌های اجرا

### فاز ۰ — رفع ریسک فنی (قفل‌کننده)

<aside>
🔒

تا پایان فاز ۰ حق نداری هیچ صفحه محتوایی بسازی. صفحه‌ای که کراول نمی‌شود رتبه نمی‌گیرد.

</aside>

- [x]  **گام ۰.۱ — شناسایی وضعیت موجود.** این فایل‌ها را بخوان و خلاصه یک‌خطی هرکدام را گزارش کن، بدون هیچ تغییری: `routes/web.php`، `app/Http/Controllers/SitemapController.php`، `app/Http/Controllers/PublicPageController.php`، `resources/views/layouts/public.blade.php`، `resources/views/pages/home.blade.php`، `public_html/.htaccess`، `public_html/robots.txt`، `app/Providers/AppServiceProvider.php`.
**معیار پذیرش:** فهرست کامل روت‌های عمومی و محل تولید تگ‌های متا گزارش شده باشد.
- [x]  **گام ۰.۲ — رفع باگ نقشه سایت.** در `SitemapController`، متدهای `index()`، `cities()` و `shops()` آکولادهای اضافه دور URL می‌گذارند و `loc` را نامعتبر می‌کنند. آکولادها را حذف کن تا خروجی یک URL مطلق ساده باشد. همچنین `lastmod` را از داده واقعی بساز (`updated_at` رکورد یا زمان تغییر فایل ویو) و `now()` را حذف کن. `changefreq` و `priority` را کامل حذف کن.
**معیار پذیرش:** خروجی `/sitemap.xml` و `/sitemap-cities.xml` XML معتبر باشد و هر `loc` دقیقاً یک URL مطلق با پیشوند `https://talalive.ir` باشد.
- [x]  **گام ۰.۳ — یکسان‌سازی پروتکل.** در `AppServiceProvider` برای محیط production متد `URL::forceScheme('https')` را اضافه کن. در `public_html/.htaccess` بلوک کامنت‌شده ریدایرکت HTTP به HTTPS را فعال کن و یک ریدایرکت ۳۰۱ از `www` به بدون `www` اضافه کن. همچنین `mod_deflate` و `mod_expires` را برای `css`، `js`، `woff2`، `png` و `webp` اضافه کن.
**معیار پذیرش:** در محیط production همه `canonical`ها و `og:url`ها با `https` تولید شوند.
- [x]  **گام ۰.۴ — پاکسازی تگ‌های متا.** در `layouts/public.blade.php` و هر ویویی که جداگانه تعریف کرده: تگ `meta keywords` را کامل حذف کن؛ مقدار `robots` را به `index, follow, max-image-preview:large, max-snippet:-1` تغییر بده؛ `og:image` و `twitter:image` را به یک سکشن قابل بازنویسی با مقدار پیش‌فرض تبدیل کن؛ `aggregateRating` را از گراف اسکیما حذف کن؛ به `Organization` فیلدهای `sameAs` و `address` اضافه کن و مقادیرشان را از کانفیگ بخوان.
**معیار پذیرش:** جستجوی `keywords` و `aggregateRating` در کل `resources/views` هیچ نتیجه‌ای ندهد.
- [x]  **گام ۰.۵ — جلوگیری از ایندکس صفحات فنی.** میدل‌ور `noindex` را به روت‌های `/tv` و `/p/{code}` اضافه کن. در `public_html/robots.txt` این خطوط را اضافه کن: `Disallow: /tv$`، `Disallow: /p/`، `Disallow: /*?key=`، `Disallow: /*?reset=`، `Disallow: /*?disconnect=`. برای صفحاتی که با پارامتر باز می‌شوند، `canonical` را بدون پارامتر تولید کن. صفحات `errors/404` و `errors/500` را هم `noindex` کن و به آن‌ها لینک بازگشت به صفحه اصلی و صفحه ستون اضافه کن.
**معیار پذیرش:** پاسخ `/tv` هدر یا تگ `noindex` داشته باشد.
- [x]  **گام ۰.۶ — رفع soft 404.** مطمئن شو روت `/{username}` برای یوزرنیم ناموجود یا غیرفعال واقعاً `abort(404)` می‌دهد، نه صفحه خالی با وضعیت ۲۰۰.
**معیار پذیرش:** درخواست یک یوزرنیم تصادفی کد وضعیت ۴۰۴ برگرداند.

### فاز ۱ — زیرساخت محتوا

- [x]  **گام ۱.۱ — ساخت منبع حقیقت کلمات.** فایل `config/seo.php` را با این ساختار بساز و **همه ۲۲ ردیف جدول ۲.۲** را در آن وارد کن. کلید هر ردیف، اسلاگ صفحه است:

```php
// config/seo.php
return [
    'pages' => [
        'digital-rate-board' => [
            'primary'   => 'نرخ نامه دیجیتال طلافروشی',
            'secondary' => ['تابلو نرخ نامه', 'نرخ نامه دیجیتالی', 'تابلو ۷ رقمه طلافروشی'],
            'title'     => 'نرخ نامه دیجیتال طلافروشی — بدون تابلوی ۷ رقمه | طلالایو',
            'desc'      => 'نرخ‌نامه دیجیتال روی تلویزیون مغازه: هفت ردیف نرخ طلا و سکه، بدون سخت‌افزار و بدون نصب. راه‌اندازی زیر ۳ دقیقه.',
            'wave'      => 1,
            'cluster'   => 'board',
            'type'      => 'service',
        ],
        // ... بقیه ۲۱ ردیف با همین ساختار
    ],
];
```

**معیار پذیرش:** `config('seo.pages')` دقیقاً ۲۲ کلید برگرداند و هیچ ردیفی `title` خالی نداشته باشد.

- [x]  **گام ۱.۲ — ساخت پارشال‌های مشترک.** این فایل‌ها را در `resources/views/partials/` بساز: `related-links.blade.php` (بلوک «مطالب مرتبط» که آرایه‌ای از سه لینک می‌گیرد)، `cta-inline.blade.php` (فراخوان میانی با عنوان و دکمه قابل تنظیم)، `schema-article.blade.php` (اسکیمای `Article` با `headline`، `author`، `datePublished`، `dateModified`، `publisher`، `image`)، `schema-service.blade.php` (اسکیمای `Service` با `provider` و `areaServed`)، `breadcrumb.blade.php` (هم HTML قابل دیدن هم اسکیمای `BreadcrumbList`).
**معیار پذیرش:** هر پنج پارشال بدون خطا رندر شوند و هیچ‌کدام `aggregateRating` تولید نکنند.
- [x]  **گام ۱.۳ — یکسان‌سازی صفحه اصلی.** `pages/home.blade.php` الان HTML کامل جدا دارد و لایه مشترک را extend نمی‌کند. آن را به `@extends('layouts.public')` تبدیل کن و فقط سکشن‌های تایتل، دیسکریپشن، اسکیما و محتوا را نگه دار. هدر و فوتر تکراری را حذف کن.
**معیار پذیرش:** صفحه اصلی هدر و فوتر یکسان با بقیه صفحات داشته باشد و تعداد خطوط فایل به‌طور محسوس کم شده باشد.
- [x]  **گام ۱.۴ — اصلاح ناوبری و فوتر.** لینک‌های ناوبری صفحه اصلی که به لنگرهای داخلی مثل `#pricing` و `#faq` اشاره می‌کنند را به روت‌های واقعی `/pricing`، `/led-vs-smart-board` و پنج ابزار تغییر بده. ابر کلمات کلیدی فوتر را کامل حذف کن و به‌جایش سه ستون بساز: «خدمات» (صفحه ستون، تعرفه، مقایسه LED، دمو)، «ابزار» (پنج ابزار)، «آموزش» (مقالات). انکرها باید طبیعی و بدون تکرار باشند.
**معیار پذیرش:** هیچ لینک ناوبری به لنگر داخلی صفحه اصلی باقی نماند و فوتر بیش از ۱۵ لینک نداشته باشد.

### فاز ۲ — موج ۱: پنج صفحه کم‌رقابت

هر گام این فاز یک صفحه کامل است: روت + متد کنترلر + ویو + اسکیما + ثبت در نقشه سایت + لینک داخلی. همه روت‌ها بالای `/{username}`.

- [x]  **گام ۲.۱ — `/guides/mazaneh-fardaei`.** مقاله حدود ۸۰۰ کلمه. باید داشته باشد: تعریف مظنه در ۴۰ کلمه اول؛ جدول مقایسه «مظنه نقدی / مظنه فردایی / مظنه جهانی» با ستون‌های تعریف، کاربرد و اثر روی نرخ ویترین؛ یک مثال عددی کامل از تبدیل مظنه به قیمت هر گرم؛ بخش «چرا مظنه فردایی برای طلافروش مهم است»؛ لینک متنی به `/tools/mesghal`، `/guides/gold-price-formula-18k` و `/smart-gold-board`. اسکیمای `Article`.
**معیار پذیرش:** صفحه رندر شود، در `sitemap-guides.xml` بیاید، و هر ۱۲ بند بخش ۳ را پاس کند.
- [x]  **گام ۲.۲ — `/guides/motefareghe-18`.** مقاله حدود ۹۰۰ کلمه. باید داشته باشد: تعریف «متفرقه» در صنف؛ جدول تفاوت «خرید متفرقه ۱۸» و «تعویض متفرقه ۱۸»؛ یک مثال فاکتور نمونه با اعداد فرضی و برچسب صریح «مثال»؛ بخش «این دو نرخ در تابلوی مغازه چطور باید نمایش داده شود»؛ لینک به `/tools/melted-gold`، `/guides/goldsmith-legal-profit` و `/digital-rate-board`. اسکیمای `Article`.
**معیار پذیرش:** مثل گام ۲.۱.
- [x]  **گام ۲.۳ — `/guides/goldsmith-legal-profit`.** مقاله حدود ۱۰۰۰ کلمه. باید داشته باشد: تفکیک سه مفهوم «اجرت ساخت»، «سود فروشنده» و «مالیات بر ارزش افزوده»؛ فرمول کامل محاسبه با مثال عددی؛ جدول اجزای فاکتور؛ اشاره به مرجع مصوبه اتحادیه با `TODO(data): لینک مصوبه رسمی`؛ لینک به `/tools/gold-price-calculator`، `/guides/gold-tax-regulations` و `/tools/wage-calculator` (لینک آخر فعلاً به `/gold-calculator` اشاره کند و در گام ۴.۱ اصلاح شود). اسکیمای `Article`.
**معیار پذیرش:** مثل گام ۲.۱.
- [x]  **گام ۲.۴ — `/digital-rate-board`.** صفحه فروش حدود ۱۲۰۰ کلمه. باید داشته باشد: `H1` با کلمه «نرخ نامه دیجیتال»؛ توضیح اینکه نرخ‌نامه دیجیتال همان کارکرد تابلوی ۷ رقمه را بدون سخت‌افزار انجام می‌دهد؛ جدول مقایسه «تابلوی ۷ رقمه سون سگمنت / نرخ‌نامه دیجیتال روی تلویزیون» با ستون‌های هزینه اولیه، تعداد ردیف، تغییر نرخ، تعمیر، مصرف برق؛ بخش «چه ردیف‌هایی نمایش داده می‌شود» با فهرست نرخ‌های صنفی از بخش ۲.۱؛ بخش سؤالات متداول با شش سؤال؛ CTA میانی و انتهایی؛ اسکیمای `Service`.
**معیار پذیرش:** مثل گام ۲.۱ و حضور در `sitemap-pages.xml`.
- [x]  **گام ۲.۵ — `/gold-board-without-device`.** صفحه فروش حدود ۱۲۰۰ کلمه. باید داشته باشد: `H1` با «تابلو طلا بدون دستگاه»؛ توضیح سه‌مرحله‌ای راه‌اندازی؛ جدول «با دستگاه / بدون دستگاه» با ستون‌های هزینه اولیه، وابستگی به سخت‌افزار، زمان راه‌اندازی، به‌روزرسانی، جابه‌جایی؛ بخش «چه تلویزیونی لازم است» با لینک به `/tv-setup-guide`؛ بخش «اگر اینترنت قطع شود چه می‌شود»؛ سؤالات متداول؛ اسکیمای `Service`.
**معیار پذیرش:** مثل گام ۲.۴.
- [x]  **گام ۲.۶ — بستن موج ۱.** از `/smart-gold-board` لینک متنی درون‌محتوایی به `/digital-rate-board` و `/gold-board-without-device` اضافه کن. از `/tools/mesghal` به `/guides/mazaneh-fardaei` و از `/tools/melted-gold` به `/guides/motefareghe-18` لینک بده. صفحه فهرست `/guides` را به‌روز کن تا سه مقاله جدید را نشان دهد. سپس `php artisan route:list` بگیر و تأیید کن هر پنج روت جدید بالای `/{username}` هستند.
**معیار پذیرش:** هر پنج صفحه حداقل دو لینک داخلی ورودی داشته باشند و هر پنج روت قبل از catch-all فهرست شوند.

### فاز ۳ — موج ۲: مقایسه رقبا و بازار LED

<aside>
⚖️

**قواعد اجباری صفحات مقایسه:** فقط واقعیت قابل اثبات از صفحه عمومی رقیب. نقاط قوت رقیب را صادقانه بنویس. بدون لوگو و اسکرین‌شات رقیب. هر جدول مقایسه باید سطر «تاریخ آخرین بازبینی» داشته باشد. موضع درست در برابر TGJU: «ابزار عمومی رایگان در برابر سامانه تخصصی صنف طلا»، نه «آن بد است».

</aside>

- [x]  **گام ۳.۱ — بازنویسی `/led-vs-smart-board`.** بازه قیمتی نادرست را حذف و با بازه مستند بخش ۲.۴ جایگزین کن. یک جدول «هزینه سه‌ساله مالکیت» با سه ستون تابلوی LED، مینی‌کیس با HDMI، و سامانه ابری اضافه کن. بخش «چرا تابلوهای LED خراب می‌شوند» با اصطلاحات فنی بخش ۲.۱ اضافه کن. لینک به `/guides/led-board-price-1405` بگذار (در گام بعد ساخته می‌شود).
**معیار پذیرش:** هیچ عدد قیمتی بدون منبع یا برچسب «تقریبی» در صفحه نماند.
- [x]  **گام ۳.۲ — ساخت `/guides/led-board-price-1405`.** مقاله مرجع قیمت. جدول قیمت بر اساس نوع ماژول (تک‌رنگ، روان تک‌رنگ، روان تمام‌رنگ) با واحد «هر متر مربع»، ستون «محدوده تقریبی» و ستون «کاربرد». بخش «چه چیزهایی در قیمت حساب نمی‌شود» (نصب، سازه، برق، تعمیر). یک هشدار صریح که قیمت‌ها نوسان دارند و تاریخ بازبینی. لینک به `/led-vs-smart-board` و `/pricing`.
**معیار پذیرش:** هر عدد قیمتی تاریخ و برچسب تقریبی داشته باشد.
- [x]  **گام ۳.۳ — ساخت `/compare/tabangohar`.** گروه روت با پیشوند `compare` بساز. جدول مقایسه با سطرهای: نوع راه‌حل، نیاز به سخت‌افزار اختصاصی، هزینه اولیه، مدل پرداخت، زمان راه‌اندازی، پوشش ارز، پوشش نقره و شمش، فرمول سود و اجرت اختصاصی، صفحه عمومی گالری، جابه‌جایی بین شعب. بخش «تابان گوهر برای چه کسی مناسب‌تر است» به‌صورت صادقانه.
**معیار پذیرش:** هیچ ادعای منفی بدون منبع، و وجود بخش نقاط قوت رقیب.
- [x]  **گام ۳.۴ — ساخت `/compare/tgju-tv`.** همان ساختار. محور تمایز: تخصصی بودن برای صنف طلا، فرمول سود و اجرت، نمایش نرخ‌های صنفی مثل تعویض متفرقه ۱۸ و آبشده نقدی، صفحه عمومی گالری، و پشتیبانی انسانی. صریحاً بنویس که TGJU رایگان است.
**معیار پذیرش:** مثل گام ۳.۳.
- [x]  **گام ۳.۵ — ساخت `/compare/tablotala`.** همان ساختار. محور تمایز: تحت وب و بدون نصب در برابر اپ استوری، به‌روزرسانی بدون آپدیت اپ، و صفحه عمومی قابل اشتراک‌گذاری.
**معیار پذیرش:** مثل گام ۳.۳.
- [x]  **گام ۳.۶ — ساخت `/online-gold-price-board`.** صفحه فروش با کلمه هدف «تابلو آنلاین قیمت طلا». تمرکز روی سرعت به‌روزرسانی و تعداد ردیف‌های قابل نمایش. یک بخش «چه نرخ‌هایی قابل نمایش است» با فهرست کامل از بخش ۲.۱. لینک به سه صفحه مقایسه.
**معیار پذیرش:** هم‌پوشانی تایتل با `/smart-gold-board` نداشته باشد.
- [x]  **گام ۳.۷ — بستن موج ۲.** هر چهار صفحه جدید را به نقشه سایت اضافه کن، از `/pricing` و `/led-vs-smart-board` به صفحات مقایسه لینک بده، و `route:list` را دوباره تأیید کن.
**معیار پذیرش:** همه روت‌های جدید بالای catch-all و در نقشه سایت.

### فاز ۴ — موج ۳: ابزارهای صنفی

- [x]  **گام ۴.۱ — ساخت `/tools/wage-calculator`.** ماشین‌حساب اجرت: ورودی وزن، عیار، درصد اجرت، درصد سود، نرخ روز؛ خروجی تفکیکی اجزای فاکتور. زیر ابزار، جدول درصد اجرت رایج به تفکیک نوع مصنوع با `TODO(data): تأیید ارقام با اتحادیه`. لینک متقابل با `/tools/gold-price-calculator` و `/guides/goldsmith-legal-profit` و اصلاح لینک موقت گام ۲.۳.
**معیار پذیرش:** ابزار بدون خطای جاوااسکریپت کار کند و خروجی با ورودی نمونه درست باشد.
- [x]  **گام ۴.۲ — ساخت `/tools/second-hand-gold`.** محاسبه‌گر طلای دست دوم: ورودی وزن، عیار، کسر افت، نرخ روز؛ خروجی قیمت خرید تخمینی. توضیح متنی درباره کسر افت و عیارسنجی. لینک به `/guides/motefareghe-18` و `/tools/melted-gold`.
**معیار پذیرش:** مثل گام ۴.۱.
- [x]  **گام ۴.۳ — ساخت `/guides/gold-hallmark-inquiry`.** مقاله آموزشی درباره انگ، ری‌گیری و شماره پاکت: انگ چیست، چه اطلاعاتی دارد، مراحل استعلام، و اینکه چه چیزی را نشان نمی‌دهد. لینک خروجی به سامانه رسمی با `TODO(data): آدرس سامانه رسمی`.
**معیار پذیرش:** هیچ ادعای اعتبارسنجی رسمی از طرف طلالایو در متن نباشد.
- [x]  **گام ۴.۴ — ارتقای `/tools/coin-bubble`.** محاسبه «ارزش ذاتی سکه» را اضافه کن و برای هر نوع سکه (امامی، بهار آزادی طرح قدیم و جدید، نیم، ربع، گرمی) یک زیربخش با توضیح کوتاه بساز. اصطلاحات «حباب گیر» و «حباب سنج» را به‌صورت طبیعی در متن بیاور.
**معیار پذیرش:** خروجی ارزش ذاتی با ورودی نمونه منطقی باشد.
- [x]  **گام ۴.۵ — ارتقای سه ابزار باقی‌مانده.** در `/tools/mesghal` تعریف مظنه نقدی، فردایی و جهانی را اضافه کن با لینک به مقاله گام ۲.۱. در `/tools/karat-converter` جدول کامل تبدیل عیار شامل ۷۰۵، ۷۴۰، ۷۵۰، ۹۹۹ و عیارهای نقره ۹۲۵ و ۹۹۵ را اضافه کن. در `/tools/melted-gold` مفهوم «آبشده نقدی» و نسبتش با نرخ اتحادیه را توضیح بده.
**معیار پذیرش:** هر سه ابزار حداقل یک جدول داده و دو لینک داخلی جدید داشته باشند.
- [x]  **گام ۴.۶ — رفع هم‌نوع‌خواری.** `/gold-calculator` را به هاب ابزارها تبدیل کن: توضیح کوتاه هر ابزار و لینک، بدون تکرار محتوای عمیق. محتوای عمیق حباب فقط در `/tools/coin-bubble` بماند. تایتل صفحه اصلی و `/smart-gold-board` را طوری تنظیم کن که هم‌پوشان نباشند: صفحه اصلی برندمحور، صفحه ستون کلمه صنفی. تفکیک `/tv-setup-guide` («چگونه وصل کنیم») از `/guides/best-tv-for-jewelry-shop` («کدام تلویزیون را بخریم») را با بازنویسی مقدمه هر دو و لینک متقابل صریح روشن کن.
**معیار پذیرش:** هیچ دو صفحه‌ای کلمه هدف اصلی یکسان نداشته باشند.

### فاز ۵ — موج ۴: بازار مجاور

- [x]  **گام ۵.۱ — ساخت `/currency-exchange-board`.** صفحه فروش مخصوص صرافی با کپی مستقل (نه کپی صفحه طلا). جدول ارزهای قابل نمایش، بخش «تفاوت نیاز صرافی با طلافروشی»، و سؤالات متداول مخصوص صرافی.
**معیار پذیرش:** حداقل ۷۰ درصد متن مستقل از صفحه ستون طلا باشد.
- [x]  **گام ۵.۲ — ساخت `/silver-bullion-board`.** صفحه فروش نقره و شمش: گرم ۹۹۹، ۹۹۵ و ۹۲۵، شمش نقره و شمش طلا. توضیح تفاوت نرخ‌گذاری نقره با طلا.
**معیار پذیرش:** مثل گام ۵.۱.
- [x]  **گام ۵.۳ — ساخت `/android-tv-gold-board`.** راهنمای فنی راه‌اندازی روی اندروید تی‌وی و باکس اندروید: مرورگر پیشنهادی، حالت تمام‌صفحه، جلوگیری از خاموش شدن صفحه، راه‌اندازی خودکار بعد از قطع برق.
**معیار پذیرش:** مراحل شماره‌دار و قابل اجرا بدون دانش فنی باشند.
- [x]  **گام ۵.۴ — ساخت دو مقاله پلتفرم.** `/guides/samsung-tizen-gold-board` و `/guides/lg-webos-gold-board` با همان ساختار گام ۵.۳ ولی مخصوص هر سیستم‌عامل. لینک متقابل بین هر سه صفحه و لینک به `/tv-setup-guide`.
**معیار پذیرش:** دو مقاله متن مشترک کپی‌شده نداشته باشند.
- [x]  **گام ۵.۵ — افزودن ارز و نقره به تابلو.** اگر ساختار داده اجازه می‌دهد، امکان نمایش ردیف‌های ارز و نقره را در تابلو فعال کن و در صفحه اصلی و صفحه ستون به آن اشاره کن. اگر نیاز به منبع داده جدید دارد، طبق قاعده ۰.۷ توقف کن و بپرس.
**معیار پذیرش:** یا قابلیت فعال شده، یا دلیل توقف دقیق گزارش شده.

### فاز ۶ — موج ۵: لوکال و اتحادیه

- [x]  **گام ۶.۱ — بازطراحی قالب صفحه شهری.** قالب `pages/cities` را طوری تغییر بده که هر شهر شش بخش داشته باشد: نرخ زنده، بلوک «نرخ اتحادیه طلا و جواهر شهر» با توضیح تفاوت نرخ محلی و مظنه تهران، معرفی راسته و بازار طلای شهر، فهرست گالری‌های فعال طلالایو در آن شهر با لینک به صفحه عمومی‌شان، سؤالات متداول اختصاصی، و اسکیمای `Service` با `areaServed`. متن‌های اختصاصی هر شهر باید از فایل داده خوانده شوند، نه هاردکد در قالب.
**معیار پذیرش:** قالب بدون محتوای یونیک شهر، آن بخش را اصلاً رندر نکند.
- [x]  **گام ۶.۲ — تکمیل سه شهر اول.** برای همدان، تهران و مشهد حداقل ۳۰۰ کلمه محتوای واقعاً اختصاصی درباره بازار طلای آن شهر بنویس. برای بقیه هفت شهر که محتوای اختصاصی ندارند، فعلاً صفحه را از نقشه سایت خارج کن و `noindex` بگذار تا در موج بعد تکمیل شوند.
**معیار پذیرش:** سه شهر کامل در نقشه سایت و هفت شهر ناقص خارج از آن.
- [x]  **گام ۶.۳ — بهینه‌سازی صفحات گالری.** برای `/{username}` تایتل الگویی «قیمت لحظه‌ای طلا و سکه — گالری {نام} در {شهر} | طلالایو» را پیاده کن، یک `H1` و متن معرفی کوتاه اضافه کن، اسکیمای `LocalBusiness` با نوع `JewelryStore` بگذار، و لینک برگشتی به صفحه شهر و صفحه اصلی اضافه کن. فقط گالری‌های فعال و دارای محتوا در نقشه سایت بمانند؛ منقضی‌ها `noindex` شوند.
**معیار پذیرش:** یک گالری نمونه تایتل الگویی درست و اسکیمای معتبر داشته باشد.
- [x]  **گام ۶.۴ — ساخت `/demo`.** صفحه پیش‌نمایش زنده تابلو بدون نیاز به ثبت‌نام، با دکمه «تابلوی مغازه خودم را بساز». از صفحه اصلی، صفحه ستون و تعرفه به آن لینک بده.
**معیار پذیرش:** صفحه بدون احراز هویت باز شود و تابلوی زنده را نشان دهد.

### فاز ۷ — موج ۶: مقیاس و اتوریتی

- [x]  **گام ۷.۱ — ساخت `/widget`.** ویجت امبدشدنی نرخ طلا به‌صورت iframe یا اسکریپت سبک، با صفحه راهنمای استفاده، تولیدکننده کد امبد، و یک خط اتریبیوشن با لینک به `talalive.ir`. حداقل دو اندازه و دو تم.
**معیار پذیرش:** کد امبد در یک فایل HTML ساده تست شده و کار کند.
- [x]  **گام ۷.۲ — ساخت `/api-docs`.** مستندات وب‌سرویس عمومی نرخ: اندپوینت‌ها، نمونه پاسخ JSON، محدودیت نرخ درخواست، و شرایط استفاده شامل الزام ذکر منبع.
**معیار پذیرش:** حداقل یک اندپوینت واقعی کار کند و نمونه پاسخ با خروجی واقعی بخواند.
- [x]  **گام ۷.۳ — ساخت `/app`.** صفحه فرود اپلیکیشن با توضیح، اسکرین‌شات و لینک دانلود. اگر اپ هنوز منتشر نشده، فرم «به من اطلاع بده» بگذار و `TODO(data): لینک استورها` علامت بزن.
**معیار پذیرش:** صفحه بدون لینک شکسته باشد.
- [x]  **گام ۷.۴ — تکمیل شهرهای باقی‌مانده.** هفت شهر باقی‌مانده را با محتوای اختصاصی کامل کن و به نقشه سایت برگردان. شهری که محتوای یونیک ندارد را برنگردان.
**معیار پذیرش:** هر شهر فعال حداقل ۳۰۰ کلمه اختصاصی داشته باشد.

### فاز ۸ — ممیزی نهایی

- [x]  **گام ۸.۱ — ممیزی خودکار.** یک اسکریپت یا تست بنویس که روی همه روت‌های عمومی این موارد را چک کند و گزارش بدهد: وجود دقیقاً یک `H1`، وجود `canonical` مطلق با `https`، نبود `meta keywords`، نبود `aggregateRating`، طول تایتل، طول دیسکریپشن، و حضور صفحه در نقشه سایت.
**معیار پذیرش:** اسکریپت اجرا شود و خروجی جدولی بدهد.
- [x]  **گام ۸.۲ — رفع خطاهای ممیزی.** هر موردی که گام ۸.۱ قرمز گزارش کرده را اصلاح کن و اسکریپت را دوباره اجرا کن.
**معیار پذیرش:** اجرای دوم هیچ خطای قرمزی نداشته باشد.
- [x]  **گام ۸.۳ — گزارش پایانی.** یک گزارش نهایی بنویس شامل: فهرست صفحات ساخته‌شده با URL، فهرست صفحات بازنویسی‌شده، فهرست همه `TODO(data)`هایی که برای صاحب کسب‌وکار باقی مانده، و مواردی که طبق قاعده ۰.۷ متوقف شدند.
**معیار پذیرش:** گزارش کامل و بدون مورد جاافتاده.

---

## ۵. قالب گزارش هر گام

بعد از هر گام دقیقاً همین قالب را بنویس، نه بیشتر:

```
گام <شماره> — <عنوان>  ✅
فایل‌های تغییریافته: <فهرست>
کاری که انجام شد: <حداکثر سه خط>
معیار پذیرش: <چطور تست کردی و نتیجه>
TODO(data) جدید: <اگر بود>
یادداشت: <اگر ایده‌ای داشتی، یک خط — بدون اجرا>
گام بعدی: <شماره>
```

سپس بدون مکث گام بعدی را شروع کن.

## ۶. تست‌های پایانی هر فاز

در پایان هر فاز این سه دستور را اجرا کن و نتیجه را گزارش بده:

```
php artisan route:list
php artisan config:clear && php artisan route:cache && php artisan view:cache
بررسی خروجی /sitemap.xml و همه نقشه‌های فرزند
```

اگر هر کدام خطا داد، قبل از رفتن به فاز بعد خطا را رفع کن.

# پایان پرامپت